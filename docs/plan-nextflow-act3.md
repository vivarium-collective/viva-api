# Nextflow dispatch, act 3: all four CD2 workloads on the Nextflow path

**Status (2026-09-09 13:20Z): 683's analysis flushed (sim 742): 8/11 with a serial 64 GB gather — `cd1_metabolomics` and `cd1_higher_order_properties` still exceed ~45 GiB alone at 10 × 8 and are the SQL-refactor targets, gated on a golden equivalence harness (reference = 739, materialising now). v2ecoli#751/#752 merged; simulator 183 building (sms-ecoli#305); the 739-shape verification run and Run 1 10 × 10 fire on it automatically.** Act 2 closed gates 4 and 1b and
put the real Run 2 shape on the path (sim 683, 10 × 8, independent founders, gather
in-campaign — still running). This act takes the other three workloads across, in
the order that risks least and proves most, **without touching any other dispatch
mechanism**: every change lands in `workflow_nf.py` / `lineage_step.py` / the
Nextflow renderer / viva-api's `K8S_NEXTFLOW` branch, or in a planner script that
never talks to the API. `lineage_ray_batch`, `mbp_dispatch`, chain dispatch and
`run_pbg.py` are out of bounds for this act. First deliverable landed:
`scripts/cd2_nextflow_dispatches.py` (the translator).

> Companion to [`plan-nextflow-dispatch.md`](plan-nextflow-dispatch.md) (act 1, the
> design and gate table) and [`plan-nextflow-act2.md`](plan-nextflow-act2.md) (act
> 2, the execution record). Same house style: status first, corrections inline.

---

## What the team runs today, and what the Nextflow path already has

Surveyed 2026-09-09 from `sms-ecoli/scripts/dispatch/cd2/run{1..4}-local.sh` (the
byte-for-byte mirrors of the real container commands), `gen_cd2_cellonly_dispatches.py`,
`build_run4_founder_caches.py`, the four `configs/cd2/run*.json`, and viva-api's
builders.

| Run | Path today | Sweep dimension today | On `workflow_nf` |
|---|---|---|---|
| **2** J3 (violacein) | `lineage_ray_batch`, ten dispatches of one (one founder cache per seed) | per-seed founders | **done as one campaign** — sim 683: ten seeds, `--independent-founders`, or ten one-seed variants with per-variant `cache_uri` |
| **1** K4 cell-only | same as Run 2 | per-seed founders (`…-founder-seed{N}` at commit `ca79191f`) | same shape as Run 2; nothing new |
| **1** K4 coupled | `mbp_dispatch` → `run_mbp_tracked.py --variant reactor-bird-coupled-batch-multigen` | per-seed founders, one job each | **not a lineage** — would need a new `MbpStep`. Deferred: works on MNP today (669 proven) |
| **4** FSS genotypes | `lineage_ray_batch`, **one dispatch per (genotype, medium)**: `{"media", "n_seeds": 1, "n_generations": 2}`; identity lives entirely in the cache `cd2-run4-carina-genotype{N}` | 42 caches × 2 media = 84; config B 16 × 2 = 32 | variants with per-variant `cache_uri`, one campaign per medium (`media` knob). **The 336-scale gather is the unknown** |
| **3** cocktail (mecillinam/sulfadiazine) | chain dispatch on `ecoli_baseline`, nested `injected_processes` (`add_processes` ×8, `process_configs`, `seed_bulk_species`, …) | **none** — `field_timeline.timeline` is `[]`; vEcoli's variants grammar is un-ported | nested block rides per variant unchanged; a dose sweep is *expressible* here via per-variant `injected_processes_patch` — a capability MNP lacks. The private vEcoli fork is a **build-time** image flag (`simulator latest --stage-private-fork --vecoli-private-commit`), so tasks inherit it with no Nextflow change |

What `workflow_nf` already declares (v2ecoli `f0274348`+): `n_seeds`, `n_generations`,
`variants[]` (each: `variant_name`, `new_genes`, `bundle_overrides`, `cache_uri`,
`injected_processes`, `config_overrides`), `cache_uri`, `independent_founders`,
`analysis_options`/`include_analysis`, `media`, `emit_paths`, `exchange_fluxes`,
`exchange_flux_basis`, `time_step`, and the rest of the 16 lineage knobs (#746).

## The translator — `scripts/cd2_nextflow_dispatches.py`

A **planner**: reads the team's workflow-schema config, applies viva-api's *own*
readers (`injected_processes_from_config`, `analysis_modules_for`) and prints the
exact `atlantis composite nextflow …` command plus the `--params` JSON. It never
calls the API, so the dispatch stays on the proven CLI path and it cannot affect the
other dispatchers. Verified on all four configs, including Run 3's nested block.

```bash
# Run 4, minimal medium, genotypes 1..42 (the strain IS the cache):
uv run python scripts/cd2_nextflow_dispatches.py --run 4 --sms-ecoli ../sms-ecoli \
  --simulator 181 --label fss-minimal --cache-commit <commit> \
  --cache-template 'cd2-run4-carina-genotype{n}' --cache-range 1-42 --media minimal
# Run 1 cell-only, ten one-seed variants from DI's founder mapping:
uv run python scripts/cd2_nextflow_dispatches.py --run 1 --sms-ecoli ../sms-ecoli \
  --simulator 181 --label k4-cellonly --founders out/founder_ensemble_mapping.json
# Run 3, a two-dose pilot (the patch content is met-eng's):
uv run python scripts/cd2_nextflow_dispatches.py --run 3 --sms-ecoli ../sms-ecoli \
  --simulator <private-fork image> --label mec-pilot --variants-json '[...]'
```

Refusals built in: `experiment_id` present in the config (it must stay absent,
sms-ecoli#235), several seeds per founder-cache variant without independent founders
(v2ecoli#693), `PENDING` founder caches, duplicate variant names.

## Phases

| # | Phase | Risk to other paths | Proves |
|---|---|---|---|
| 0 | **Run 2 depth + auto-gather** — sim 683 (running) | none | 8 generations on parquet; the gather at 10 sweeps |
| 1 | **Run 4 pilot**: 3 genotypes × 1 seed × 2 gens on 181, `media=minimal`, then `minimal_plus_tryptophan` | none (translator + existing knobs) | per-variant caches fetch; `media` reaches the lineages; `multivariant` analyses gather across variants |
| 2 | **Run 4 at scale**: 42 × 1 × 8 per medium (84 lineages/campaign), then 4 seeds if wanted (168 → 336) | none; watch the gather's memory (`_scaled_memory`, 32 GB base) and `list_jobs` pagination in the cancel reconciler (already paginated, #481) | the 336-scale gather |
| 3 | **Run 1 cell-only**: 10 one-seed variants on the `ca79191f` founder caches, 10 gens | none | the K4 arm on this path; 11/11 KPIs on 181 |
| 4 | **Run 3**: build a simulator with `--stage-private-fork --vecoli-private-commit <Chris/Mia's commit>`; 1 × 1 pilot with the nested block; then a dose grid via `injected_processes_patch` | none (image flag exists; per-variant injection exists) | the cocktail on this path; the first dose sweep anywhere |
| 5 | **Run 1 coupled** (optional): an `MbpStep` wrapping `run_mbp_tracked.py` | additive only | parity with `mbp_dispatch` |

Gates, in act-1 style: a phase closes only when the campaign's `analysis/` publishes,
every expected `lineage_seed=`/`variant=` partition is present with real history
(>1 column, >0 rows — #475's criterion), and the KPI analyses report `ok`.

## Known risks, stated up front

- **Cache/image commit mismatch.** A `cache_uri` fetch bypasses the schema-3 key, but
  a cache built under an older v2ecoli can still be refused at load. 683 runs a
  `2fddfcb8` cache under image 172 (`e4db5e67`), so at least that span works.
- **The gather at 84–336 sweeps.** 574 needed 32 GB for 3; scaling is unmeasured.
  `_scaled_memory` retries at 2×; the resource label is overridable per dispatch.
- **Run 3's private fork** is a build-time choice per simulator; a campaign on the
  wrong image fails at `!ParameterSerializer[...]` resolution, loudly.
- **`media` names** must be a condition in the cache's `saved_media`.

**Sequence:** [act 1](plan-nextflow-dispatch.md) (design + go/no-go gates) → [act 2](plan-nextflow-act2.md) (gate 4 / 1b closed, blockers, Run 2 on the path) → this document.

## Progress log

| date | event |
|---|---|
| 2026-09-09 | Survey of the four workloads' real dispatch shapes (mirrors, generators, cache builders). Run 4's identity is the cache; Run 3 has no dose sweep today; Run 1 coupled is not a lineage |
| 2026-09-09 | Translator written with 9 tests; verified the API's own config readers on all four CD2 configs. Phase 1 pilot command generated for genotypes 1–3 |
| 2026-09-09 | **Phase 1 fired: sim 724** (`run4-pilot-3g`, simulator 181): genotype caches 1–3 from `ray-parca-cache/b3b4a628a2aea8afe6f9524b6de65c657ff23475/` (the prefix holding all 43 Run 4 caches; `9ce3135` holds 42 more), 1 seed × 2 gens, `media=minimal`, the config's full `multivariant`/`multiseed`/`multigeneration` analysis set, exchange fluxes on. Command generated by the translator verbatim. Translator #533 merged (`30f9ade7`), plan #534 merged |
| 2026-09-09 | **sim 724 FAILED 03:25Z — infrastructure, not the path.** Nextflow rendered three variants and submitted `parca_v0/v1/v2` (cache fetches); `parca_v0` succeeded; `parca_v1`/`parca_v2` failed 4× each with `CannotPullContainerError: … no space left on device` and the head Job hit its backoff limit. Diagnosed by SSM: the spot host running seven of 683's lineages had a **30 GB root at 98 %** (17 GB = the running lineages' own writable layers, 11 GB = one simulator image, nothing reclaimable); the other host was at 79 % with 6.6 GB free — also too small for an 11 GB pull. Launch template `lt-06346ca86b6cec29b` has no block-device mapping |
| 2026-09-09 | Fix applied online (no restart): `modify-volume` both roots to 120 GB + `growpart`/`xfs_growfs` via SSM → 98 % → 26 %, 79 % → 21 %. 683's lineages were one generation from ENOSPC. Durable fix filed as sms-cdk#48; team told on #166. **Re-fired as sim 725** (same translator command) |
| 2026-09-09 | Also this hour, not act 3 but relevant: my viva-api#525 under-run guard refused every chain-dispatch job (Run 3's 720/721): `ecoli_baseline` declares `n_generations` yet runs `-n 1` + `stop_at_division` per generation. @AlexPatrie root-caused and fixed it (#536, exempt `stop_at_division`, bump to 0.9.129). `run_pbg.py` is staged from the api pod, so it needs a roll — overlays bumped and rolling now |
| 2026-09-09 | **sim 725 FAILED 03:40Z — the first act-3 risk, exactly as stated.** Cache fetches all succeeded (disk fix confirmed), render correct (`parca_v{0,1,2}` → `lineage_v{0,1,2}_s0`), then `StaleCacheError: inputs_hash mismatch; files differ: [_helpers.py, ecoli_baseline.py, reactor_bird_coupled.py …]` in every lineage: the caches are from sms-ecoli `b3b4a628` (simulator 166, v2ecoli `2e54e95a`); 181 is `ad5f2f3a`. Same rule as MNP. 166 predates #736/#739/#742 so cannot gather. Route: rebuild the 42 caches at the current commit (`build_run4_founder_caches.py`: one ParCa + 42 deep copies). Asked Alex on #166 (he owns the numbering). 0.9.129 rolled and verified (`stop_at_division` marker on the pod) — Run 3 re-fire unblocked |
| 2026-09-09 | **Team state 02:55–05:00Z** (read from GitHub): eagmon confirmed **Run 4 = 4 seeds × 8 gens × 84 genotypes** (the 116 "done" dispatches were a 1×2 pilot) → Alex rebuilding the chassis (Dispatch 729 on 181; its ParCa landed in the `cd2-run1-k4-chassis/` variant slot, 181's stock slot is untouched — no collision) then the 42 genotype caches at `6299ba5`; he asked me to hold off minting a parallel cache set, so the Nextflow Run 4 campaign will use his rebuilt caches on 181. **Run 2 reopened** (681 had one shared founder, 4 gens) → Alex re-fired 10×10 with per-seed founders (728/730/731/733); Chris: nothing needs rebuilding. **Run 3**: sms-ecoli#299 (Alex) generates the real 36-combo mecillinam×sulfadiazine configs, one dispatch per point; ~~eagmon's audit says the ports are faithful but *no drug dose reaches the cell*~~ — **corrected by eagmon 05:18Z**: that audit ran on a Sep-3 worktree against the empty-timeline base config; on current main delivery works end to end (#653 seeds the field keys, #299 bakes the dose) — mecillinam[p] 0 → ~10,954 counts confirmed at runtime. What remains is config/model work: wire sulfadiazine's transport block (a1), port the DHPS kill into v2ecoli's FBA (~10 lines, scoped), then fire — gated on eagmon's go; Bug B (`AmbiguousLookupError`) confirmed fixed on 0.9.129. **Run 1 coupled 10/10 complete**, all content-verified. eagmon opened viva-api#542 for the same ParCa-list 500 as my #541 (reconciled on his PR) |
| 2026-09-09 | **Pivot to phase 3 while Run 4 waits.** Run 1 cell-only's ten founder caches (`…founder-seed{0..9}` at `ca79191f`) match simulator **173** (v2ecoli `e4db5e67`: has #736/#739/#742, not #746). Translator gained: fluxes inside every variant's injection block (#544, merged) and `--legacy-image` (#546) after **sim 732 failed at render** — on a pre-#746 image `to_document` *raises* `KeyError: unknown override(s): ['exchange_flux_basis', 'exchange_fluxes']` rather than swallowing them. **Re-fired as sim 734** (founders seed0/seed1 × 1 × 2 gens, legacy mode) |
| 2026-09-09 | 683 at 05:00Z: 7 of 10 lineages finished, s1/s6/s7 still running (7 h 15 m in); the 32 GB gather follows |
| 2026-09-09 | github-sync 05:23Z. **Run 3 has no infrastructure blocker any more** (eagmon 05:22Z): delivery seam closed at runtime, Bug B closed (727 reached division), Bug A pin = vEcoli-private master tip `22c0bff2` (param block + topology fix; needed only for `!ParameterSerializer` configs — the native configs now carry literals, so the private fork is moot on current main). Path: a1 sulfadiazine transport wiring + DHPS FBA port (cd2-runs-ptools) → fire 36 × 4 × 20. My #548 (one-campaign shape of that sweep) is acknowledged by Alex as correctly held. Alex: Dispatch 736 = Run 3 re-verify on 0.9.130. **Run 4**: Alex reports all 42 genotype caches rebuilt against chassis `parca_dataset_id=240` and the first real 4 × 8 dispatch (genotype 1 / minimal, `num_nodes=4`) in flight; they live under the SHORT prefix `ray-parca-cache/6299ba5/` (42 caches, 04:59Z, built in-container at 181's commit). eagmon #301: zero the 51 BAD_RXNS in redux (Run 4 feasible-region fix) |
| 2026-09-09 | **sim 737 fired 05:27Z** — Run 4 pilot, third attempt: genotypes 1–3 from Alex's rebuilt caches at `ray-parca-cache/6299ba5/` (same commit as image 181, so schema-3 should accept them), 1 seed × 2 gens, `media=minimal`. Reads only; no collision with Alex's 4 × 8 MNP test |
| 2026-09-09 | github-sync 06:03Z: Alex merged viva-api#550 (`run_pbg` elapsed time from the batch runner's own `wall_s`) and opened #551 → **0.9.131**, which also rolls my #541 (ParCa-list 500 fix, merged) — `atlantis parca list` works again once it lands. #548 (one-campaign Run 3 sweep) merged. Translator now covers all four runs' shapes: founders (Runs 1/2), genotype caches + media (Run 4), dose combos (Run 3), `--legacy-image`. Campaigns: 683 on its last lineages, 734 (Run 1 pilot, 173) and 737 (Run 4 pilot on the rebuilt caches, 181) both mid-lineage |
| 2026-09-09 | **sim 734 FAILED 06:40Z — blocker 8.** Both lineages SUCCEEDED on their own founder caches (sweep_v0_s0 = 491 MB, gens 0–1 real; the second sweep's publish was aborted with the workflow), then `Process 'analysis' input file name collision … cache`: the gather stages every variant's ParCa cache (#742) and every ParCa task emits a directory literally named `cache`. 574/679/683 had one variant each. Same class as blocker 5. **Fix: v2ecoli#750** — `path "cache_v*", type: "dir"` at class level + `cache_v{i}` per node (`cache_*` would have matched the `cache_dir.json` manifest — the structural test from blocker 6 caught that), `founder_sim_data` follows; 48 tests incl. both preview compiles |
| 2026-09-09 | **sim 737** (Run 4 pilot on Alex's rebuilt `6299ba5/` caches, image 181): all three lineages running with no `StaleCacheError` — the rebuilt caches verify on 181. Its gather will collide the same way; re-fire after #750 lands. The caches stay valid on the next image (`workflow_nf.py` is not in the `inputs_hash` list). Run 1 cell-only can also move to a #746+ image: Alex's per-seed K4 founder caches exist under 181's prefix too |
| 2026-09-09 | **sim 737 FAILED 07:05Z on blocker 8**, as predicted — but genotypes 1 and 2 published full 2-generation histories (540 MB / 536 MB) from Alex's rebuilt `6299ba5/` caches on 181: the rebuilt caches verify. 0.9.131 applied (Alex's overlay bump had sat unrolled for an hour): `/version` 0.9.131, ParCa list 200 again |
| 2026-09-09 | **v2ecoli#750 merged** (`033a4a17`); sms-ecoli#302 re-pins (`ad5f2f3a` → `033a4a17`, verified against origin/main; trap: sms-ecoli's pyproject has four 40-hex `rev` pins — extract the `v2ecoli =` line only) → **simulator 182** (`30b604b`, 11 min). Re-fired: **sim 739** (Run 4 pilot, genotypes 1–3 × 1 × 2, `media=minimal`) and **sim 740** (Run 1 cell-only pilot, founders seed0/1 from the per-seed K4 caches under `6299ba5…/`, exchange fluxes via the #746 knobs — no legacy mode). Both gathers will be the first multi-variant gathers ever to stage |
| 2026-09-09 | 683 correction: its lineages were NOT retry-free — s1 and s6 died exit 1 at ~02:35Z (the 98 %-disk window) and s7 exit 137 at 03:47Z; Nextflow retried all three from scratch (s6's retry succeeded; s1/s7 retries due ~08:30–09:00Z, then the gather). A retried lineage costs its full wall time |
| 2026-09-09 | **sim 683 COMPLETED 09:37:54Z** (Nextflow Run 2: 10 independent founders × 8 gens, simulator 172, ~12 h wall incl. three from-scratch lineage retries). Published 27.5 GB / 1,895 objects; all ten `lineage_seed=` partitions × generations 0–7 (2.2–3.1 GB each). `analysis.json` **PARTIAL 5/11**: ptools singles ×3 `ok`, multigeneration ×2 `ok`; **all six multiseed modules `error`** — five DuckDB `OutOfMemoryException` (`max_temp_directory_size` 63.7 GiB exhausted; 22.3 GiB pinned) and `cd1_exchange_fluxes`'s expected binder error on 172. The gather at 10 × 8 (27 GB history) does not fit 32 GB for the multiseed cd1 modules: the scale risk, real at 10 seeds. Fix belongs to the analysis runner / cd1 multiseed modules (DuckDB threads + temp sizing, or per-seed aggregation), not the dispatch |
| 2026-09-09 | **sim 740 COMPLETED 09:53Z** (Run 1 K4 cell-only pilot: founders seed0/seed1 × 1 × 2 gens on simulator 182) and **sim 739 COMPLETED 09:55Z** (Run 4 pilot: genotypes 1–3 on the rebuilt `6299ba5/` caches, `media=minimal`, simulator 182) — the first multi-variant campaigns whose gathers completed on this path (blocker 8 fixed). Contents (partitions, `analysis/`, `media` applied, exchange-flux columns) still to verify: the AWS SSO token expired ~09:45Z and took the tunnel with it |
| 2026-09-09 | **Gather fixes in review**: v2ecoli#751 — `runner.max_workers` + `runner.duckdb.{threads,temp_dir,max_temp_directory_size}` in the analyze config (the runner already set `memory_limit` from the cgroup and allowed ~1 module per 4 GiB, which is why 683's five multiseed modules ran together); v2ecoli#752 — one gather per variant (`analysis_v{i}`, own sweep + cache, all scales but multivariant) plus a campaign `analysis` node only for multivariant modules; single-variant campaigns unchanged; every gather `max_workers: 1`, task-local spill. No SQL changed. **Verification protocol committed**: re-run 739's shape on the next image and diff every module's output under `analysis_v{i}/` against 739's `analysis/`, cell by cell |
| 2026-09-09 | **sms-cdk**: the 200 GB gp3 root (`0ed87ef`, 2026-08-07) is on main and deployed to prod's `smscdk-batch`, but `smsvpctest-batch` was last deployed 2026-06-12 → dev's task CEs still get the AMI's 30 GB. Durable fix = `cdk diff` + `cdk deploy smsvpctest-batch` (user). Noted on sms-cdk#48 |
| 2026-09-09 | eagmon's owner map (12:13Z): claimed **683's analysis** and **Run 1 cell-only** (blocker 8 cleared). **sim 741 fired 12:20Z** = 683's campaign resumed on image 172 with identical params incl. the explicit `experiment_id` (ten lineages cached), gather at 64 GB / 8 CPUs / 6 h and `V2E_ANALYSIS_MAX_WORKERS=1` via `-process.beforeScript` — the existing runner honours that env var, so the modules run one at a time without waiting for #751. Run 3: sms-ecoli#303 (sulfadiazine transport wiring) + v2ecoli#753 (DHPS inhibition) open, eagmon-reviewed as faithful ports |
| 2026-09-09 | 12:30–12:45Z: **v2ecoli#752 merged** (`76fd6b26`) and **#751** green (merge queued); **Run 3 code complete on main** — sms-ecoli#303 (sulfadiazine transport wiring) + v2ecoli#753 (DHPS inhibition), both eagmon-reviewed as faithful ports; the 36-combo sweep is fireable (eagmon/Alex), and `--run3-sweep` renders it as one Nextflow campaign. Alex: **the gen-3 `AsyncZarrBufferWriter` failure is NOT fixed by #520** — Dispatch 723 (Run 1 cell-only, `mbp_dispatch`) and 735 (Run 4's first real 4 × 8, `lineage_ray_batch`) both died at generation 2→3 with `RAY_OUT_S3` correctly set; he is holding the 83 remaining Run 4 dispatches. Posted the unblock: `lineage_ray_batch` defaults `emitter: both`, the writer runs only for xarray/both, `LineageStep` defaults `parquet`, and 683 went 8 gens parquet-only — `"emitter": "parquet"` in the MNP params avoids it |
| 2026-09-09 | **742** (683's gather flush by resume): attempt 1 and 2 died in `aws s3 cp` staging the ten sweeps — `[Errno 28] No space left on device` — on a fresh m7i.metal-24xl with the launch template's 30 GB root (100 % during staging). Grew it online to 120 GB; attempt 3 running on it. A gather's disk need = whole history staged locally + DuckDB spill, so the `smsvpctest-batch` deploy (200 GB root) is now on the critical path for every gather |
| 2026-09-09 | Chain armed: merge #751 → re-pin sms-ecoli → simulator 183 → **sim `run4-pilot-3g-split`** (739's exact shape, for the cell-by-cell before/after diff of every module under `analysis_v{i}/` + `analysis/` vs 739's `analysis/`) and **Run 1 cell-only 10 × 10** (ten founders on their `6299ba5…` caches, one gather per founder) |
| 2026-09-09 | **sim 742 COMPLETED 13:1xZ** — 683's gather re-run by resuming its campaign on 172 (ten lineages cached; attempts 1–2 died staging onto a 30 GB host, attempt 3 on the hand-grown host): 8 CPUs / 64 GB / `V2E_ANALYSIS_MAX_WORKERS=1`. **8/11**: `cd1_fluxomics`, `cd1_proteomics`, `cd1_transcriptomics` now `ok` (1.5–4.7 MB TSVs); `cd1_metabolomics` and `cd1_higher_order_properties` still `OutOfMemoryException` at **44.8 GiB each, running alone**; `cd1_exchange_fluxes` binder error (172). Quantitative conclusion: serialising modules fixes three of five; two modules need query-side work at 10 × 8 |
| 2026-09-09 | **Correctness rule for any DuckDB SQL change (user)**: a quantitative before/after equivalence test, not just timing. Plan: a golden harness — run every module once on a fixed reference (739's three sweeps + their genotype simData, being materialised locally; the proving-set history is absent and 574's prefix is gone), pin outputs, compare cell by cell (exact for counts/strings, ≤ 1e-9 relative for floats); `compare_gathers.py` for real-campaign before/after on S3. v2ecoli#754 (per-module `runtime` block: wall time, peak RSS, DuckDB memory/temp) supplies the performance side |
| 2026-09-09 | **Golden equivalence harness ran (13:30Z), and it earned its keep.** Reference = the local copy of 739 (3 genotypes × 2 cells × 2,537 timesteps + their genotype simData); every cd1 module run per genotype through `ANALYSIS_REGISTRY` exactly as the gather does; TSVs compared cell by cell **paired by row key and column name** at 1e-9. `main` vs the fix branch at its new default (chunk 8): **140,652 cells, 0 mismatches**; `main` vs the branch at chunk 1 (every cell its own batch — the multi-batch path incl. HOP on real data): **140,652 cells, 0 mismatches**; HOP unchunked vs chunked at 1 and 8: 60 + 60 cells, 0. **It also caught a real bug in my own #747 module**: `cd1_exchange_fluxes`'s per-cell `GROUP BY` had no `ORDER BY`, so identical code + input produced *swapped cell columns* in 1 of 3 runs (hash-aggregate order). Values were never wrong; column↔cell assignment was nondeterministic. Fixed with `ORDER BY` the id columns; 3 reruns hash identically. All three in **v2ecoli#756** (chunk default 100→8 because a 10×8 campaign is 80 cells < 100 = ONE batch, which is why 742's two modules needed 44.8 GiB; HOP through `run_chunked`; the ORDER BY) with the table in the PR. Synthetic unit test: HOP chunked at 1/2/100 byte-identical to unchunked on a 5-cell history (uneven batches [2, 2, 1]). #754 (runtime metrics) failed CI once — its serial==parallel test compared the measured runtime block — fixed. Both on merge-on-green watchers |
| 2026-09-09 | **Chain done 13:30Z**: #751 merged → sms-ecoli#305 (`385abec`, v2ecoli `0a301c18` = #751 runner knobs + #752 per-variant gathers + #753 DHPS) → **simulator 183** → **sim 745** = `run4-pilot-3g-split` (739's exact shape on 183: per-variant `analysis_v{i}/` + campaign `analysis/`, for the cell-by-cell diff against 739's single `analysis/` via `compare_gathers.py`) and **sim 746** = Run 1 K4 cell-only **10 founders × 1 seed × 10 gens** on 183. Both in their ParCa (cache-fetch) phase at 13:40Z. 0.9.132 (Alex's #558: stage `seed_overrides[*].cache_dir` on the Ray path; #560 overlay bump) is rolling on smsvpctest at the same time — the api pod restarts under the two live campaigns; the scheduler's boot reconcile is what keeps them tracked |
| 2026-09-09 | **sms-cdk verified by `cdk diff --all` (read-only, `DEPLOY_ENV=stanford-vpc-test`)**: the IaC is NOT stale — main `e7e4840` already has the 200 GB root (`0ed87ef`); the only smsvpctest drift is `smsvpctest-batch` missing that one resource (`[+] BlockDeviceMappings` on `BatchComputeLaunchTemplate`); every other stack "no differences". **Caveat before deploying**: the four task CEs pin `latestVersionNumber` and the stack lacks `replaceComputeEnvironment: false`, so CloudFormation *replaces* them (prod's 2026-08-07 events: "requires the creation of a new physical resource", old CE deleted 20 s after the queue re-pointed) — anything RUNNING on `smsvpctest-vecoli-task-*` (a Nextflow head included) loses its host; the Ray queues are untouched. So: deploy in the idle window after 745/746, ~2 min. Posted on sms-cdk#48 |
| 2026-09-09 | **Run 3 mechanism is moving again (eagmon, 13:12–13:37Z)**: v2ecoli#755 *reverts* #753's in-`Metabolism` DHPS logic in favour of a generic `imposed_flux_bounds` input store (`{reaction_id: {upper_bound?, lower_bound?}}`, drug-agnostic), and sms-ecoli#306 wires the sulfadiazine DHPS rate law as an *injected* process writing through it (volume basis fixed in `caeb1aef`; both LGTM'd). Sequence he states: #755 → #306 → bump the sms-ecoli v2ecoli pin. Consequence for act 3: **simulator 183 carries the #753 form that is about to be reverted** — Run 3 on the Nextflow path should wait for the post-#306 pin (**simulator 184**), and the translator's `--run3-sweep` injection block must include the new injected DHPS process (it takes its bounds seam from the composite, so this is an injection-block change, not a knob) |
| 2026-09-09 | **745 and 746 FAILED 13:37Z on `StaleCacheError` — act-3 risk 1 again, from a different direction.** `v2ecoli/library/sim_data.py` is in `cache_version.INPUT_FILES`; v2ecoli#753 (DHPS inhibition, merged 13:0xZ) added 9 lines to it, so **every ParCa cache in S3 is stale for any image containing #753** — simulator 183 (`0a301c18`) is the first, and both campaigns died on their first cache fetch (`files differ: ['v2ecoli/library/sim_data.py']`; 745 at `cache_v0`, 746 at `cache_v6`). 181/182 and Alex's Ray dispatches are unaffected. eagmon's #755 revert leaves `sim_data.py` byte-identical to pre-#753 (`git diff 76fd6b26 origin/generic-flux-bound-hook -- v2ecoli/library/sim_data.py` is empty) and `processes/metabolism.py` is not hashed → the post-#755 pin (**simulator 184**) re-validates all caches; 745/746 re-fire on it. Rule added to the cache memory: before pinning a commit for a cache-consuming run, diff `INPUT_FILES` against the cache's commit. Also corrected on #166: eagmon's "744 stalled 4 h" was `aws s3 ls` printing EDT — 744's job started 13:07Z and wrote gens 0–1 by 13:36Z; not stalled |
| 2026-09-09 | With 745/746 down, **both task queues are idle (13:48Z) → the `cdk deploy smsvpctest-batch` window is open now**; the deploy replaces the four task CEs (see sms-cdk#48) and touches nothing on the Ray queues, where Alex's 747 and the ten Run 3 chain jobs run |
| 2026-09-09 | **sms-cdk#48 deployed 13:51–13:53Z** (`smsvpctest-batch` from main `e7e4840`, task queues idle): launch template `lt-06346ca86b6cec29b` **v5 = 200 GB gp3 encrypted**; the four task CEs replaced as predicted and the queues re-pointed (`…task-amd64` → `Amd64SpotComputeEnv-lC1fXfiTH1qg5NjK`); Ray queues untouched. **Not in the diff: the submit node was replaced too** (`latestAmazonLinux2023` resolves the current AMI at synth) — it is the SSM-tunnel bastion; the restart loop re-attached within a minute. Evidence on sms-cdk#48; follow-up PR adds `replaceComputeEnvironment: false`. The Opus subagent that ran the deploy hit the session limit right after `cdk deploy` returned; verification and the comment were done from here |
| 2026-09-09 | **v2ecoli#756 merged 14:26Z** (`04e15db4`): cd1 chunk default 8, HOP chunked, exchange_fluxes ORDER BY — the DuckDB work with its 140,652-cell equivalence proof. #754 (runtime block) green, merging behind a branch update. **eagmon re-pinned sms-ecoli himself** (#307, `74ffb930` 14:09Z → v2ecoli `8cf9a4bc` = #755) and merged #301 (zero 51 BAD_RXNS), #304 (well-mixed field floor), #308 (opt-in plain FBA objective, `objective_formulation`, classic byte-unchanged); #309 (Run 3 scale 4 seeds × 20 gens) open+LGTM'd — matches the translator's `--run3-sweep` shape. `chain_184` will therefore re-pin past his pin to pick up #756 (+#754 if merged by 14:40Z), build **simulator 184**, and — the cdk sentinel is now written — re-fire `run4-pilot-3g-split` and `run1-k4-cellonly-10x10` |
| 2026-09-09 | **Team, 13:50–14:22Z**: Alex confirmed 744 healthy (gens 0–1 success markers, writes at 13:49Z) and that his `6299ba5/` caches are unaffected by the #753 hash gap; **Run 3 chain-dispatch Bug B definitively closed** — dispatch 743 (mecillinam well-mixed, 10 seeds × 5 gens, simulator 181) 50/50 generation jobs succeeded. **eagmon reproduced the zarr zero-arrays root cause**: the XArrayEmitter buffers rows (buffer.size=600) and only persists on buffer flush — a durability gap in the emitter, not S3/codec — which also explains the gen-3 `_check_group` crash; `emitter: parquet` remains the Run 4 unblock |
| 2026-09-09 | **Chris ACCEPTED Nextflow sim 683 as Run 2's deliverable (14:40Z)** — graded against his three criteria: right design (ten distinct founders, gen-0 cell_mass 1029–1269 fg; 80/80 cells; explicit experiment_id), readouts legible (both exchange fluxes; violacein non-zero on 99.93 % of 217,745 rows), KPIs within a fifth of the team's spread standard (vio 0.1169 ± 0.0155 vs 681's 0.1040; glucose −4.973; µ 0.627/h). Report-card analysis starting on the parquet. **The first science-accepted CD2 deliverable off the Nextflow path.** His ask: always say "Nextflow sim 683" — the MNP job of the same shape is dead. What still gates the *ptools* outputs (not the verdict): `cd1_metabolomics` + `cd1_higher_order_properties` (OOM) and `cd1_exchange_fluxes` (binder error on 172) → the gather-only re-analysis below |
| 2026-09-09 | **Simulator 184 built (`4bd7c82`, sms-ecoli#310: v2ecoli `fa99c6e9` = #755 + #756 + #754; injected DHPS from #306); image pushed 14:53Z** — the build had already started at 14:42Z on the merge, so `simulator latest` found it done. cdk sentinel present → **sim 748** = `run4-pilot-3g-split` (739's shape on 184: per-variant gathers + campaign gather, cd1 chunk 8, runtime block) and **sim 749** = `run1-k4-cellonly-10x10` (ten founders × 1 × 10 gens) fired 14:54Z; both running, task queues on the new 200 GB hosts for the first time. Next: `compare_gathers.py <739 prefix> <748 prefix>` cell by cell, and the `runtime` numbers |
| 2026-09-09 | **v2ecoli#757 merged 14:50Z (Alex; `analysis_runner.main()` accepted no `s3://` sweep_dir)** and his sms-ecoli#311 re-pins → **simulator 185** is the image for the **683 gather-only re-analysis**. Scoped: the API's re-analysis endpoint routes Ray-backend sims to a K8s Job (10 GiB ceiling, sim_data from the bare commit cache) — unusable for a 45 GiB gather over ten founder caches. Design: a Batch container job via the existing campaign-analysis submitter with an image override, the campaign's published sweeps as sweep_dir, one founder's simData as sim_data, the gather resource label (32 GB → 64 GB) and #751's runner knobs. viva-api#564 merged: `--run3-combos` for a one-combo Run 3 pilot |
| 2026-09-09 | **683 re-gather DONE locally (16:01Z) — 11/11 analysis outputs now exist for Nextflow sim 683, and #756 is proven at the real 80-cell scale.** Alex's suggestion (local checkout, no image) + a 128 GiB laptop: the ten published sweep trees (587 history parquet, 25 GiB) + 683's own `parca_v0` simData, through `ANALYSIS_REGISTRY` with the campaign's exact analysis_options, v2ecoli `b9ff70b5`, chunk 8. All six cd1 modules ok on 80 cells; **`cd1_metabolomics` 4.1 GB and `cd1_higher_order_properties` 3.9 GB peak** (44.8 GiB OOM on 742); `cd1_exchange_fluxes` 54 × 80 (glucose −5.12 ± 0.60, violacein 0.119 ± 0.015 — consistent with Chris's parquet read). **Equivalence vs 742's three TSVs, header-keyed at 1e-9: 232,142 + 353,830 + 356,782 = 942,754 cells, 0 mismatches.** Published at `…/sim172-run2-j3-10x8-founders-4626/analysis/local-regather-b9ff70b5/` (+PROVENANCE.json); posted on #166. Harness: `$T/golden683_run.py` + `drive683.sh` + `tsv_diff.py` (trap: DuckDB allows ONE `**` per glob — list the ten history dirs). Wall times (~6 s/module) are local NVMe, not a Batch number; memory is the transferable result |
| 2026-09-09 | **sim 751 = the first Run 3 on the Nextflow path** (15:56Z, simulator 185): `--run3-sweep --run3-combos 21` (mec 0.001 + sulf 0.01 mM) × 1 seed × **5 gens** (the sweep's dose onset is 10,000 s ≈ gen 4, so a 1-gen pilot would end before either drug), chassis `--cache-uri ray-parca-cache/6299ba5/` (the bare media-minimal chassis 743 used). Injection block carries all nine Run 3 processes incl. `sulfadiazine-dhps-inhibition` (kcat 0.38, km 0.00782, k_i 0.00515, cell_density 1100). Head pod submitted `parca_v0` at 16:00Z. **Caveat**: the API-side `--simulation-config` is the translator's default violacein config for every run — Run 3's own top-level knobs (time_step/emitter/media) are not carried, only the injection block; fix if the pilot's output traces to them. Task-CE capacity read: Spot 2000 vCPU / OnDemand 256 → Run 4 full (168 lineages × 4 vCPU = 672) fits one campaign per medium |
| 2026-09-09 | **Blocker 9 (mine): sim 748 FAILED at the per-variant gather (17:17Z).** All three lineages published (41 objects each), then `analysis_v2` died 4× with `FileNotFoundError: analysis.config.json` — #752's per-variant nodes are staged as `analysis_v{i}.config.json` (the renderer pins `stageAs` to the node name) but `AnalysisTaskStep.nextflow_script()` hard-coded the single-node name. **v2ecoli#760**: `--config "${config_json}"` + a rendered-script structural test (fails on main). Lesson: `-preview`/`-stub-run` never execute scripts; every staged-name contract needs a structural test on the rendered file. **749 will hit the same error at its ten per-founder gathers** — letting the lineages finish (the expensive, valuable part) and gathering locally as with 683. Run 4 full on Nextflow = hedge only (Alex's 84/84 MNP campaign on parquet is in flight; 744 past gen 5) |
| 2026-09-09 | **Run 3 on Nextflow, first real finding (sim 751, 15:56Z; retry identical).** Gen 0 ran to division; the daughter's first tick died in `pg_maturation.next_update` → `solve_ivp` "y0 must be finite" — the daughter's `listeners.mass.cell_mass` was **NaN** (a zero mass raises ZeroDivisionError one line earlier). Dose not yet delivered. Chain dispatch (Alex's dispatch 750, same combo, simulator 186) crosses the boundary fine (checkpoint restart, no in-process division). Traced to the in-process division: `steps/division.py` builds daughter docs inside the division tick (mass zeroed → `seed_mass_listener`) and the nested `run_steps` in the traceback runs the bridged `pg-maturation` on that daughter in the same tick; a plain `baseline()` + `seed_mass_listener` on the chassis gives a finite 1266 fg locally, the injected build could not be reproduced outside the image. **sms-ecoli#314** (eagmon's module, for review): skip a non-integrable tick (zero/NaN mass or non-finite y0) with a RuntimeWarning naming the state; every other tick byte-unchanged; 66/66. Also: the config's top-level `flow` block is inert on every native path (inject.py docstring) — injected steps run at the END of each tick in `add_processes` order, so the DHPS bound lags metabolism by one tick on chain dispatch too; the `[inject]` log only names processes that add NEW stores (dhps/gillespie/well_mixed_field silence is normal); the translator's API-side config is the violacein test config (Run 3's `emitter`/`single_daughters`/`skip_baseline`/`suffix_time` not carried). Sim **870** = same pilot with `LINEAGE_DEBUG_DIVISION=1` (its line cannot print — the exception is re-raised first; kept for the division-signal record). Sim ids jumped 751→870: Alex's 83 Run 4 + 36 Run 3 dispatches |
| 2026-09-09 | **v2ecoli#758 re-keyed every ParCa cache (17:28Z)** — `reactor_bird_coupled.py` is one of the 17 `INPUT_FILES`; its bytes changed (+7/−1), so any image ≥ `514e0cc8` fails `inputs_hash` on all 42 genotype caches, the K4/J3 founders and the chassis. Measured, not argued: caches record `f0109bf9…` for that file; the file hashes `f0109bf9…` at 033a4a17/b9ff70b5 and `9c75bb77…` at 514e0cc8. Chris's "schema 3 on both sides → no rebuild" was wrong (schema number ≠ inputs_hash) and retracted. **Alex's decision: no rebuild; the `V2ECOLI_SKIP_CACHE_VERIFY=1` hatch instead** (biology unchanged by an emit-path edit). #315/#316 (pin bumps to 514e0cc8) closed each other, then #316 closed as superseded — **#317** (my chain) moved the pin to `f99b0ca5` (#760 + #758). Durable fix = eagmon's `INPUT_FILES` design (#735). My chain's cache gate had extracted the list with a regex (18 ≠ 17) — now imports `INPUT_FILES` and diffs per file |
| 2026-09-09 | **viva-api#568 → 0.9.133 live (18:26Z)**: per-dispatch `task_env` passthrough (top-level `extra_params.task_env` + per-block; strict validation; MNP `shared_env`, container/chain `env`, Nextflow `containerOptions --env` via nextflow_deploy.py:175; CLI `--task-env NAME=VALUE` on simulation run / composite run / composite nextflow; 46 tests) — built by a fork subagent, reviewed, released with the proven chain. Deploy trap recorded: the chain grepped the OLD (terminating) pod and reported marker 0 / image 0.9.132; re-verified on the new pod `api-784b7565fb-tdg6h`. Also: **sms-ecoli#314 merged** (pg-maturation guard, eagmon LGTM); **870 failed identically ×4** (debug line cannot print); **sim 871** = 748's shape on simulator 187 with the hatch — all three cache fetches verified, lineages running (the hatch works on Nextflow); eagmon's **v2ecoli#761** (zarr durability fix) in review |
| 2026-09-09 | **sim 871 COMPLETED 21:20Z** (739's exact shape on simulator 187 = sms-ecoli `64d3720` / v2ecoli `f99b0ca5`, `V2ECOLI_SKIP_CACHE_VERIFY=1` via `-process.beforeScript`): the first campaign whose **per-variant gathers (#752) and the #760 staged-config fix ran end to end** — `analysis_v0/1/2/` + campaign `analysis/`, `compare_gathers.py` pairs 37/37 files with 739, `ptools_overview` and `cd1_higher_order_properties` identical cell-for-cell, `runtime` block present. Measured: per-variant gather 2 m 19 s–2 m 30 s at 10.5–12.2 GB peak RSS (`ptools_proteins`/`metabolites` ~11.7 GB, `overview` ~38 s), campaign gather 1 m 9 s at 7 GB; lineages 2 h 0 m–2 h 25 m each. Blocker 9 closed |
| 2026-09-09 | **871 vs 739: the ptools proteins/RNA/metabolites/rxns tables differ from 739 (expected — sms-ecoli#301 zeroes 51 `BAD_RXNS` on 187; dry mass 484 → 472 fg at t = 800 s) AND are identical across 871's three genotypes.** Traced, not argued: dispatch is correct (each `parca_v{i}` fetched its own `6299ba5/…genotype{i}/`, each lineage staged its own `cache_v{i}`, the dills differ in `ecoli-polypeptide-initiation.translation_efficiencies`); the histories of genotypes 0 and 1 are bit-identical through the end of generation 1 (242/247 columns; the 5 others are FBA solver noise). Root cause is **cache construction**: the violacein operon `NG-TU-001` has `actual_rna_synth_prob` ≈ 9.6e-9 (max 0.02), mRNA 0 at every timestep, all Vio enzymes 0 — in **both** 739 and 871. All 42 `ray-parca-cache/6299ba5/cd2-run4-carina-genotype*/` caches (Alex's rebuild, 04:59–05:01Z) record `requested.expression = 6.07` — the design config's **log-space exponent** (`exp: logspace 6.07` ⇒ 10^6.07 = 1,174,897.55) passed as the linear factor that `POST /parca/new-gene-cache` forwards verbatim (`simulation_service_ray.py:1259`). The previous set (`b3b4a628…/`, 09-08) and 683's founders used 1,174,897.55 and express the operon (683: synth prob 2.5e-3, thousands of Vio enzymes). The KPIs did not show it because `metabolism_redux.py` substitutes 1000 enzymes for every zero Vio slot (`VIO_nE[VIO_nE == 0] = 1000`), so `violacein_production_flux` reads ~1.2e-3–1.9e-3 with no enzymes. Posted on #166 for Alex (his `minimal` arm, 7/42 on these caches, measures no genotype axis); the placeholder is flagged for Eran/Chris, not proposed as a fix. **Phase 2 stays held until the caches are rebuilt** |
| 2026-09-09 | Team (Alex, 21:22Z): Run 1 cell-only re-fired as Dispatch 893 (723's shape, queued behind Run 4); ~~coupled re-fire 883–892 healthy past gen 3~~ **— RETRACTED 2026-09-10 17:37Z by Alex, who verified it against the ensemble's own stored data after @cplong90 filed sms-ecoli#332: the reactor→cell feed was dead throughout. Dispatch 883 seed 0 has `boundary__external__GLC` climbing 122 mM (t=1 s) → 281,120 mM (t=2,534 s) instead of being regulated, and `boundary__external__OXYGEN-MOLECULE` literally `inf` at every timepoint. A clean exit status never caught it. This ensemble is NOT valid evidence for anything and must not be cited. `database_id 949` was checked the same way and is genuinely unaffected (GLC steady ~22 mM, oxygen finite and stable), so the Vignette-1 proof stands.**; Run 3 cancelled sweep cost 346 node-hours over 544 jobs (19 % of 2,880); Run 4 `minimal` 7/42 on the `6299ba5/` caches (see above); with-trp `GLP_UNBND` closed as a model-data decision (`media_recipes.tsv` TRP `[Infinity]`). sim 749 (Run 1 10 × 10 on 184) still running |
| 2026-09-09 | **Cache rebuild attempted and refused by the data (22:13–22:20Z).** User: rebuild the 42 genotype caches with the linear expression into a fresh prefix. 42 × `POST /parca/new-gene-cache` (dataset 240, `expression 1174897.5549395303`, each genotype's own vectors, labels `cd2-run4-carina-lin-genotype{n}`) → all 42 FAILED: `ValueError: no new-gene cistrons in this sim_data`. The endpoint stages from the **bare** `ray-parca-cache/6299ba5/` slot, and that slot was overwritten at **12:44Z** by a bare chassis (`parca_state.provenance.json`: `new_genes: off`, v2ecoli ad5f2f3a) — after Alex's 04:59Z induction. The violacein chassis at 6299ba5 no longer exists; nothing was written under the new labels. **Trap for the record:** `submit_new_gene_cache_job` hard-codes `stage_s3=cache_s3_uri(commit)`; a chassis in a variant slot cannot be the source without a `source_variant` option |
| 2026-09-09 | **The corrected set already exists: `ray-parca-cache/b3b4a628a2aea8afe6f9524b6de65c657ff23475/cd2-run4-carina-genotype{1..42}/`** (09-08 07:28Z, dataset 222 / simulator 166): `expression 1174897.55`, all 42 `rel_trl_eff_adj`/`rel_exp_adj` vectors identical to the 6299ba5 set (checked file by file), complete on S3 (six files, uniform sizes). Dataset 222's ParCa options = 240's; v2ecoli `2e54e95a..ad5f2f3a` touches no `parca/`, `sim_data.py`, `processes/`, `perturbations/` (only composites/workflow → `inputs_hash` re-key, hence `V2ECOLI_SKIP_CACHE_VERIFY=1`, already required on every image since #758). The b3b4a628 stock slot still holds the violacein chassis (provenance `new_genes: violacein_MG1655_M5`) if a re-induction is ever wanted. **sim 894 fired 22:31Z** = 871's exact shape (genotypes 1–3 × 1 × 2, `minimal`, simulator 187, skip-verify) on the b3b4a628 caches; acceptance = `NG-TU-001` mRNA > 0, Vio monomers > 0, the three histories diverge (871's were bit-identical). Posted on #166 for Alex with the one-line change for his `minimal` arm |
| 2026-09-09 | **Alex's two Run 4 gaps (22:24Z) explained from the pod log + `hpcrun` (22:50Z).** Submission order broke id parity at genotype 23: 830 = g23-minimal 16:31:36Z, 831 = g24-minimal 16:31:46Z … 868 = g42-withtrp; g23-withtrp went in last as 869 (16:35:43Z), so from g24 on odd ids = `minimal`. The 18:46:44–54Z cancel sweep was by odd id (799 → 869): correct for g7–22, but for g24–42 it cancelled the 19 **minimal** dispatches (CANCELLED 2 h 15 m after submission, not seconds) and left the 19 with-trp ones running until his 21:59–22:13Z stop. Cancel by `experiment_id` substring, never by id arithmetic. **The 6299ba5 chassis overwrite is pinned**: dispatch 729 (chassis rebuild, dataset 240, `run_parca`) wrote the violacein chassis into the bare slot 04:30–04:42Z; 743 (Run 3 verify, dataset 239, `run_parca`, 12:28–14:08Z) wrote a bare chassis over it at 12:44Z. **Any chain dispatch with `run_parca` rewrites its commit's bare slot — the new-gene-cache endpoint's only source.** Posted on #166 |
| 2026-09-09 | **Team 22:42–22:43Z (eagmon's session)**: Run 4 tryptophan medium routed to Chris as a model/data decision (matches the hand-off); Alex given two go-aheads — (1) Run 3 as a "clean 20-gen re-dispatch, no code change needed", (2) re-point the 42 minimal genotypes at the `b3b4a628…` caches after sim 894 passes, checking gen1→2 progression. **(1) is wrong on chain dispatch, re-verified on main (22:55Z):** `_lineage_offset` is initialised 0.0 per `LineageProcess` (`lineage.py:292`) and grows only within one process (`:913`); chain dispatch runs one process per generation (`simulation_service_ray.py:2239-2264`, no offset anywhere in viva-api; `run_pbg.py:525`), so the 10,000 s dose never fires at any generation count — the cancelled 544-job sweep was that control. Nextflow runs the 20 gens in one process → dose at gen 4. Posted the two options (Nextflow on 189 now, or thread the offset through chain, ~2 h). Also flagged that 894 (2 gens) cannot show the gen1→2 boundary |
| 2026-09-09 | **23:01–23:03Z: eagmon's session retracted the chain go-ahead** ("Jim's right … scratch that") and split Run 3 into NEAR-TERM (Nextflow, me + cd2-runs-ptools: I fire, cd2 verifies the dose/kill readout; production fire = Eran's go) and DURABLE (govcloud-workbench to make chain dispatch run a whole lineage in one `LineageProcess` — option B, theirs). Asked for a combo and the S3 coordinates. **sim 898 fired 23:08Z** = Run 3 dosed pilot: combo 21 (mec 0.001 × sulf 0.01 mM), 1 seed × 5 gens, simulator 189, chassis = bare `6299ba5/` stock cache, skip-verify. Prefix `vecoli-output/sim189-run3-pilot-combo21-5g-ad75/`; dose at cumulative 10,000 s ≈ gen 4. Posted the prefix, hive layout, sim_data location and the offset arithmetic for cd2's readout. Phase 4's first real dosed run on this path (751/870 died at the first division; #314 is in 189) |
| 2026-09-09 | **23:06–23:20Z: the Run 4 with_trp failure is a viva-api dispatch bug, and so is the whole MNP Run 4 campaign.** eagmon's session (update-from-vecoli) reframed GLP_UNBND: the fork always swaps metabolism → redux on trp media and the traceback is in *classic* `metabolism.py`, so the deployed config must be dropping the swap. Checked, not assumed: 744/787/788 all store `swap_processes` at the top level (the committed shape); 787's Batch log traceback is `v2ecoli/processes/metabolism.py:906 → modular_fba.py → nf_glpk.py`; **744's history (the "clean 8-gen" run) carries the classic-only FBA listeners and none of redux's** (`violacein_production_flux`, `estimated_exchange_dmdt__*`), while 739/871's Nextflow histories carry them. Cause: `_submit_multi_node_composite` passes `multi_node_dispatch.params` verbatim and never calls `injected_processes_from_config` (only chain dispatch and `_sim_command` do); `lineage_ray_batch` accepts `injected_processes` (v2ecoli#663) but nobody sends it. So every MNP Run 4 dispatch, both arms, ran classic FBA with no violacein ODE, on silent-operon caches — two independent reasons none of it is a Run 4 result; and my earlier "communication path faithful" verdict on with_trp was true of the classic path the dispatcher wrongly built. **Fix: viva-api#576** (`_thread_injected_processes_into_params`; explicit params win; 2 tests; 295 pass) → **0.9.134**, deploy chain running. The `b3b4a628…` re-fire must wait for it. Posted on #166 |
| 2026-09-09 | **viva-api 0.9.134 live 23:29Z** (PR #576 merged 23:23Z, build run 34416680906, pod `api-795d6d4454-xtwmw`, marker `_thread_injected_processes_into_params`=2, `/version` 0.9.134, tagged): MNP dispatches now carry the config's swap/add/exclude. Told #166 the Run 4 `minimal` re-fire on `b3b4a628…` is unblocked after 894, with the redux-vs-classic column check as acceptance. eagmon opened **viva-api#578** ("Chain dispatch: run each seed's whole lineage in ONE LineageProcess", the durable Run 3 dose fix) at 23:31Z — reviewing |
| 2026-09-09 | **23:48Z eagmon's P0 list + config-faithfulness table.** Configs faithful on mechanism/parameters; risks are downstream (deferred strain/induction/dose pieces — the with_trp swap-drop class). P0s: (1) `antibiotic_mic.py` filters on per-generation time → MIC = lowest dose for every genotype, needs an owner (not mine); (2) `pg_cellwall`/`lysed` reset per generation; (3) `well_mixed_field` dose re-applied per generation. **Verified (2) and (3) on the Nextflow path (23:58Z):** `select_carry_daughter` carries only `bulk/unique/environment/boundary` (`lineage.py:38`); injected agent-root stores (`fields`, `imposed_flux_bounds`, the wall) come back fresh each generation; `field_timeline` fire-once entries re-fire on the first tick of every post-onset generation (fresh instance + cumulative offset) into a zero-seeded `fields`; the Run 3 config has no `decay_rates`, so both drugs are fire-once → **pool reset to full at each division, no cross-generation wall damage**. Consequence for **sim 898**: it tests onset timing, transport, DHPS inhibition and the within-generation growth response, not a cumulative kill curve. Fix = the carry set in `lineage.py` (plumbing, fork-faithful); assigned to govcloud-workbench by eagmon; offered to take it. Hardening PRs up: sms-ecoli#320 (config-effective checker + S3 verifier with `--expect-swap`/`--expect-expression`, redux first-tick guard), v2ecoli#762/#763/#764 |
| 2026-09-10 | **00:23Z: two go-aheads from Eran (via eagmon's session), both mine**: (1) the lineage carry-set fix in v2ecoli (`select_carry_daughter`/`apply_carry_state` AND `divide_cell`/Division — the inner Division rebuilds daughters from `baseline()` and overlays only the four classic stores, so the reset happens there first); (2) the with_trp ParCa rebuild + a `source_variant` option on `POST /parca/new-gene-cache`. cd2 owns the MIC-analysis P0. **viva-api#581 opened 00:40Z** (`source_variant`; tests; spec regenerated with the env overrides; no bump — #578 holds 0.9.135, now rebased and mergeable). Carry-set fix delegated to an Opus subagent with the verified facts (fork divides the wall via `divide_pg_cellwall`, copies environment; typed-node overlay must keep `_type`). Merged tonight by the team: sms-ecoli#320 (config-effective checker, S3 verifier with `--expect-swap`/`--expect-expression`, redux first-tick guard), v2ecoli#762 (run_identity). ecoli-sources#18 (finite TRP recipe) still open |
| 2026-09-10 | **sim 894 COMPLETED 00:37Z — PASS. Phase 1's science half closes.** 871's shape on 187 with the `b3b4a628…` caches (expression 1,174,897): `NG-TU-001` mRNA 51/51/44 at t = 1199 s (871: 0), Vio enzymes in the thousands (871: 0), `violacein_production_flux` 0.0145/0.0176/0.0071 and genotype-dependent (871: 0.0019 from the placeholder, identical), the three genotypes diverge from gen 0 (170/247 columns, 871: 5), redux columns present, classic absent; gen 1 keeps expressing; all gathers ran (199 objects). Only the caches changed. Posted on #166 with the re-fire recipe for the 42 `minimal` genotypes. **#578 merged 00:33Z (0.9.135, no overlay bump) → PR #583 bumps both stanford-test overlays; deploy chain running.** #581 rebased over #578, mergeable, awaiting eagmon's review |
| 2026-09-10 | **0.9.135 live 00:49Z** (PR #583 overlays; build run 34422490138; pod `api-f9f468bc7-qhf59`, marker `_seed_lineage_command`=3, `/version` 0.9.135, tagged): chain dispatch now runs each seed's whole lineage in one `LineageProcess` (#578), so a `field_timeline` dose can fire on chain too — untested for the dose until a chain pilot runs. **Carry-set fix: v2ecoli#765 opened 00:48Z** (eagmon reviewer): extra agent-root stores carried across generations with a typed-node-safe merge, `register_store_divider` / `register_carried_listener_path` registries, `divide_cell(dividers=)`; 9 tests. Limits, stated in the PR: the inner Division step cannot see the extras (fixed input ports) so the runner carries them from the ≤1-tick-stale mother snapshot; `pg_cellwall` copied until sms-ecoli registers `divide_pg_cellwall` (one line); RNG/ppgpp/attenuation stores deliberately not carried. Nothing running today is affected; the next simulator build picks it up after merge |
| 2026-09-10 | **sim 898 live progress (01:05Z), after eagmon's session twice called it failed-to-launch** (the output prefix holds only the dispatch scripts until the single lineage task exits — 871/894 looked the same mid-run). Lineage Batch job `efd63607…` RUNNING since 23:11:17Z; CloudWatch (`/aws/batch/job`): gen 0 divided t=2,528 s (714.7 fg, 2 chr), gen 1 1,734 s (670.9 fg), gen 2 2,981 s (1,113 fg, 4 chr), gen 3 3,237 s (1,210 fg, 4 chr) → cumulative 10,480 s; **the 10,000 s onset was crossed inside gen 3** (local t ≈ 2,757 s); gen 4 started 00:59Z with offset 10,480 (re-fires on first tick under today's code). Every daughter's first tick survived (#314 works). Render verified from the head pod: bare 6299ba5 chassis (`media_id minimal`, `new_genes off`), 9 injected processes, timeline `[[10000,{mecillinam:0.001}],[10000,{sulfadiazine:0.01}]]`, no decay. Lesson for the record: on Nextflow, progress is in the lineage task's CloudWatch stream, never in S3 before the task ends |
| 2026-09-10 | **v2ecoli#765 merged 01:22Z** (eagmon: "LGTM, the right general fix"; two confirmations asked: (a) a real multigen run shows no stale carried store, (b) the wall divider actually halves). **sms-ecoli#323 opened 01:40Z** (eagmon reviewer): pin v2ecoli → `06f99b98` (#765 + #762/#763/#764) and adopt the registries — `pg_maturation` registers `divide_pg_cellwall` under `STORE_DIVIDERS["pg_cellwall"]` (split, not copy), `pg_shape` registers the `lysed` leaf and restores the latch in `next_update`; 3 tests, `tests/sms_modules` 305 pass. Plan for (a): build the simulator from #323, re-fire 898's shape (combo 21, 1 × 5) as the before/after pair for dose persistence + wall damage. Also tonight: sms-ecoli#321 (MIC cumulative time, cd2) merged; sms-ecoli#322 (pre-dispatch config gate) and v2ecoli#766 (seed aliasing) open |
| 2026-09-10 | **sim 898 COMPLETED 01:30Z — the first dosed Run 3 result on any path.** 127 objects, gens 0–4 (division at 2,528 / 1,734 / 2,981 / 3,237 / 3,069 s local). Sulfadiazine reaches the cell: `CPD-20940[c]` 0 → 16,227 at gen 2's division tick, 10,918 → 17,643 across gen 3, 9,490 → 17,526 across gen 4 (`[p]` ~¼ of that); growth to division unchanged (1,113 / 1,210 / 1,202 fg); `lysed` False; DHPS-flux readout left to cd2. **Finding 1 (v2ecoli, mine): the onset fired ~2,900 s of simulated time early** — the drug appears at gen 2 local t ≈ 2,800 s = 3,600 × 2 + 2,800, because `_run_until_division` booked `_gen_elapsed += interval` (the 3,600 s window) regardless of the division time, so `lineage_time_offset` = 3,600 × n, every gen `timed_out=True`, summary durations 3,600. **Fix = v2ecoli#767** (`_elapsed_after_run`: daughters' stamped `global_time`, else the inner clock, else the window; 3 tests). Chain (#578) inherits it. **Finding 2** (not diagnosed): intracellular drug 16,227 at gen 2's division → 0 at gen 3 t = 0, but 9,490 (≈ half) at gen 4 t = 0 — a bulk store, so not the #765 class; likely the division tick's transport write vs the bulk snapshot. Reading rule for 898: dosed interval = last ~180 s of gen 2 + gens 3–4; any tool computing onset as 3,600 × n agrees with the wrong clock. Posted on #166 |
| 2026-09-10 | **01:38Z eagmon: 0.9.135's whole-lineage chain jobs would be refused by the run_pbg under-run guard** (`_seed_lineage_command` emits `n_generations=N`, `-n 1`, no `stop_at_division`; the guard exempted only `stop_at_division`). Confirmed in code; **viva-api#593 → 0.9.136** exempts `ecoli_baseline`'s batch shape (`BatchBaselineRunner` is a Step: one update = the whole lineage), keeps the contract for the single-cell shape and `lineage_ray_batch`, rewrites the stale docstring; 2 tests incl. the exact chain command through the guard; 553 green. Deploy chain running. Also merged upstream: v2ecoli#766 (seed aliasing). eagmon verified the 898 dose-fire independently with DuckDB (agrees: jump at ~2,800 s of gen 2). Four RFCs opened in viva-api (#588–#591: config-conservation, one resolved document over the dispatch paths, run-provenance, truthful status) |
| 2026-09-10 | **viva-api#581 merged 02:02Z (eagmon LGTM at 00:33Z, missed for 90 min — check PR reviews, not just #166) → 0.9.137 live 02:17Z** (pod `api-f75b8cdbd-cvmkj`, tagged): `POST /api/v1/parca/new-gene-cache` accepts `source_variant`, staging the chassis from `<commit>/<source_variant>/` instead of the last-writer-wins bare slot (the overwrite that failed the 42 re-inductions). **v2ecoli#767 open** (rebased over #766; `_elapsed_after_run` books each generation's real elapsed time from the daughters' `global_time`, fixing the 3,600 s × n `lineage_time_offset` behind 898's ~2,900 s early onset) and **sms-ecoli#323 open** — both green, blocked on eagmon's review (reminder posted 02:23Z at Jim's request). Reviews are required because the repos' branch rules require them, not by team habit. |
| 2026-09-10 | **with_trp: Chris (person) confirmed the intent 02:20Z** — tryptophan is a violacein precursor supplement (240 mM stock at a 0.1 volume fraction, ~24 mM effective), not a carbon source; the unbounded exchange we ported in its place is his suspect for the crash. ecoli-sources#18 (merged 00:16Z) makes the recipe finite, but shipped `TRP_supplement.tsv` **without its `reference_bundle.tsv` row**, so v2ecoli's `test_media_registration` fails on the #18 commit itself. **ecoli-sources#19** (one manifest row; the genotype tests pin the row count, 135 → 136) and **v2ecoli#768** (draft: ecoli-sources pin → #19's tip, `TRP_supplement.tsv` registered in `LIST_OF_DICT_FILENAMES`, recipes sha updated; the pin lives in v2ecoli because a second pin in sms-ecoli is a uv lock conflict, tried and reverted on #323). Media recipes are ParCa-time data (`make_media.py` → `saved_media` → the metabolism config in `sim_data_cache.dill`), so nothing running changes: the order is #19 → #768 → sms-ecoli re-pin → chassis into a variant slot (Alex's) → 42 inductions with `source_variant` (mine). Monitor `bg8z578r3` watches all four PRs. |
| 2026-09-10 | **02:43Z eagmon reviewed LGTM and merged all three**: v2ecoli#767 (`c2ba69ed`, real per-generation elapsed time), sms-ecoli#323 (`b98c5f79`, #765 pin + wall divider + lysed latch), ecoli-sources#19 (`86607c4d`, the TRP_supplement manifest row). Go-ahead restated: re-pin onto #767, one simulator, re-fire 898's shape on both paths as the before/after; both chain and Nextflow open since #593. **Done by 02:46Z**: v2ecoli#768 off draft (rebased over #767, pinned to `86607c4d`), **sms-ecoli#324** (pin v2ecoli → `c2ba69ed`), **simulator 191** building from that branch (`996294b`). Re-fire plan: Nextflow = 898's translator invocation on 191 (combo 21, 1×5, bare `6299ba5/`, skip-verify); chain = `atlantis simulation run` on `configs/cd2/run3_sweep/combo_21.json --generations 5 --seeds 1`. Expected: onset at cumulative 10,000 s (inside gen 3, local ≈2,757 s on 898's division times) and the wall/latch carried across division. Alex (02:44Z) flagged v2ecoli's `pbg-ketchup` dependency name vs the repo's `viva-ketchup` rename — `uv lock` still resolved for #768; unclaimed. Alex claimed the with_trp chassis build (02:22Z). |
| 2026-09-10 | **Simulator 191 built 02:56Z (11 min) → 898's shape re-fired on both paths.** Nextflow **sim 943** (02:57Z, `sim191-run3-pilot-combo21-5g-767-ca53`): 898's exact invocation (combo 21, 1×5, bare `6299ba5/` via `--cache-uri`, skip-verify) — only the code differs (#762–#767). Chain **sim 944** (02:59Z, `sim191-run3-pilot-combo21-5g-767-chain-3a22`): same combo config, `--run-parca` (fresh ParCa at `996294b`; the chain CLI exposes no cache override) — cumulative test, not a strict A/B. Readout vs 898: onset at cumulative 10,000 s (898 fired ~2,900 s early), `pg_cellwall`/`lysed` carried across division. **Two chain-CLI traps** (cost two 4xx submissions): `--config-filename` is relative to `configs/` (server rejects the prefix); `--run-parca` defaults OFF and the Ray backend refuses `run_parca=false` with a 500 wrapping a 400. Also 02:55Z: confirmed to Alex we are not on the workbench UI (his redeploy ask) and corrected his 10–20 min workbench build estimate — `build-and-push.yml` measures ~4 min (last five runs); simulator builds are the ~11 min ones. |
| 2026-09-10 | **943 AND 944 FAILED in generation 1 (03:30Z / 03:35Z) — a #765 regression in our carry set, not the model.** Gen 0 intact on both (division 2,528 s, identical daughter bulk totals). 943 (Nextflow): `NegativeCountsError … partitioned_counts: RNA0-300[c] in ecoli-polypeptide-elongation (-1)` within the first ticks after division. 944 (chain): `Failed to meet molecule limits with ppGpp reactions` before its first 400 s chunk. 898 (pre-#765) ran 5 gens. **Mechanism**: #765 carries every non-core, non-excluded, non-edge agent-root store from the mother snapshot onto the rebuilt daughter; `NON_CARRIED_ROOT_KEYS` missed the per-tick partition roots `request`/`allocate` (`partition.py:45` groups them with `process`/`listeners`; each Requester overwrites only its own entry, the Allocator loops over every key present) → the daughter's first tick partitions a half cell against the mother's stale full-size demands. **v2ecoli#769** (eagmon reviewer): adds both keys + regression test; 71 lineage tests pass, full gate running. **Bisect/after images**: simulator 192 from sms-ecoli `b98c5f79` (#765 without #766/#767, control, 1×2 fires on build) and simulator 193 from `bisect/pin-769-fix` (`c233c7a`, #769's head; 1×5 on both paths fires on build) — `$T/fire_when_built.sh`. Reading recipe: chain container jobs log to `smsvpctest-ray-batch-RayBatchLogs2D18AAFC-vjj0f55ZeO5i` (not `/aws/batch/job`); `aws logs … --output text` tab-joins lines. #769 flags the deny-list policy (`central_fluxes`, `pinned_flux_targets` would be carried too) without deciding it. |
| 2026-09-10 | **Pilots on the bisect/fix images (03:50–03:55Z)**: sim **945** = 1×2 Nextflow on simulator 192 (`b98c5f79`, #765-only control); sim **946** = 1×5 Nextflow on simulator 193 (`c233c7a` = #769's head; `sim193-run3-pilot-combo21-5g-769-ad0c`); sim **947** = 1×5 chain on 193 (`…-chain-b9eb`, `--run-parca`). **Sim 749 (Run 1 K4 cell-only 10×10, simulator 184) finished its lineage side 03:14Z**: all 10 variants × gens 0–9 published under `vecoli-output/sim184-run1-k4-cellonly-10x10-a91d/`; marked failed by `analysis_v8` = blocker 9 (`analysis.config.json` per-variant staging, pre-#760 image; 22:57Z). Data intact; gather offline with the 683 harness. Posted as the counterpoint to Alex's Dispatch 901 (MNP zarr `_check_group` race at gen 3→4 on lineage_seed=1; 7/9 other seeds fine). **Trap found**: 943's head kept retrying its deterministically-failing lineage (fresh 30-min gen 0 each time, `errorStrategy retry`) while the API showed it running, holding a task-queue slot with 945/946 RUNNABLE — cancelled 943 04:05Z. Follow-up: retry only reclaim-class exits. Also corrected sms-ecoli#328's premise (viva-api's `POST /api/v1/parca/new-gene-cache` does build genotype caches on GovCloud; offered to wire the same induction gate there). |
| 2026-09-10 | **BISECT CONFIRMED (04:42Z): #765's carry set alone breaks generation 1.** Sim 945 (simulator 192 = sms-ecoli `b98c5f79`, v2ecoli `06f99b98`: #765 without #766/#767) reproduced 898's generation 0 bit-for-bit (division 2,528 s, 714.7 fg) and died 11 s after division: `NegativeCountsError … glnW-tRNA[c] in ecoli-polypeptide-elongation (-1)` — 943's class. #766 (re-seeding) explains the 721.8 fg on 943/944/946 and is not implicated; #767 not implicated. 945 cancelled 04:45Z (deterministic failure; no point letting Nextflow retry). **946 (simulator 193 = v2ecoli#769's head, Nextflow)** divided 04:27:50Z and was still in generation 1 at 04:45Z with no traceback (broken images die in 7–11 s). **947 (chain, same fix)**: seed lineage running after ParCa — the JobScheduler handed off correctly across the 04:15Z API pod restart Alex's #601 rollout caused (shared.env rolls api/ptools/workbench together). **sms-ecoli#324 closed unmerged (Jim, 04:33Z)**: one post-#769 pin to v2ecoli main (#765+#766+#767+#769+#770) replaces it. Full gate on #769's branch: 2,625 passed; all 74 failures/errors environmental or pre-existing on main (classification on the PR). |
| 2026-09-10 | **#769 ACCEPTED at generation 1 on both paths (04:48Z).** Simulator 193 (#769's head): sim 946 (Nextflow, bare `6299ba5/`) gen 0 t=2,528 s / 721.8 fg → **gen 1 divided t=1,674 s / 636.8 fg**; sim 947 (chain, fresh ParCa `c233c7a`) gen 0 t=2,746 s / 654.2 fg → **gen 1 divided**. Broken images (943/944/945) died 7–11 s after gen 0's division on the same shape. Both legs in generation 2 of 5; onset readout (cumulative 10,000 s) with gens 3–4, ~06:00Z. Asked Eran to merge #769 on this evidence; then ONE sms-ecoli pin to v2ecoli main (#765+#766+#767+#769+#770), one simulator from main, production Run 3 fire. Observability lessons (Jim's question, 04:50Z): the bisect was confirmation, not discovery — the gaps are (1) no test sees the real agent-root store set (#765's tests use fakes), (2) a deny-list over an open root set, (3) the carry seam logs nothing, (4) first observable is one full generation (~30 min) away on the cluster, (5) #765 merged and pinned before the multigen run its review asked for. Proposed, not started: static root-port classification test; per-generation carry log line; allow-list; 1×2 pilot before a lineage/division change becomes the main pin; fail-fast retry policy for deterministic post-division failures. |
| 2026-09-10 | **946 and 947 COMPLETED 5 generations on #769 (05:39Z / 05:34Z) — but the dose never fired.** Readout from the bulk history (`CPD-20940[e/p/c]`, `mecillinam[p]`): 898 (pre-#767) onset at gen 2 local 2,801 s = cumulative **7,063 s (−2,937 s)**, persisting into gens 3–4; **946/947: zero drug in every generation** (cumulative reached 12,411 / 13,847 s). Cause (947's `summary.json`: `duration: 0.0` for all five generations): #767's `_elapsed_after_run` precedence (1) returns the rebuilt daughter's stamped `global_time`, which is 0.0, so `_gen_elapsed` = 0, `_lineage_offset` (`lineage.py:1056`) never advances, and `field_timeline`'s 10,000 s onset is unreachable within one generation. The inner clock (precedence 2; 2,528 s on gen 0) is the right value. #769 (carry set) stands verified; #767 needs a one-line follow-up (ignore a stamped 0 / prefer the inner clock) + regression test + a 1×5 re-run before the post-#769 pin goes to main. Posted on #166 for Eran (fix) and Chris (do not read 946/947 as dosed). Observability lesson: the offset is never logged; a `generation_start` event with `lineage_offset` would have shown 0.0 at gen 1. |
| 2026-09-10 | **#767 was wrong in the other direction — 946/947 never dosed (05:50Z readout, `duration 0.0` ×5 in 947's summary.json).** The Division step rebuilds each daughter from `baseline()` with `global_time: 0.0`; #767's precedence (1) returned that stamp, so `_gen_elapsed` never advanced and the 10,000 s onset was unreachable ("3,600 × n, early" on 898 → "0, never" on 946/947). My bug; **v2ecoli#771** (eagmon reviewer): a daughter stamp counts only if it advances the clock, else the inner clock (which stops at the division signal = division time). **Acceptance restated**: 946/947 prove #769 (carry set) only — not Run 3's dosing; told Chris not to read them as dosed. **Next measurement without waiting on reviews** (Jim's branch-chain method): v2ecoli `bisect/769-plus-771` (`9add1a29`) → sms-ecoli `bisect/pin-769-771` (`703da96`) → **simulator 194** (building 05:55Z) → 898's shape 1×5 on both paths fires on build (`$T/fire_194.sh`), then the same duckdb onset readout (expect cumulative 10,000 s). The post-#769 pin to main waits for that number. Lesson for the observability plan: #767's tests stamped division times on fake daughters; the real Division step does not — a rendered-composite test or the planned `generation_end.duration` event would have caught it. **Observability plan approved 05:45Z** → `docs/plan-observability.md` (#605); PR-A (viva-api `feat/nextflow-head-poller`) and PR-B (process-bigraph `feat/events`) in progress. |
| 2026-09-10 | **#771 readout on simulator 194 (07:50Z): the chain path doses at exactly cumulative 10,001 s; the Nextflow path still never doses.** 952 (chain): `summary.json` durations = division times (2,528 / 2,746 / 3,116 / 2,833 / 2,626); `CPD-20940[c]`/`[p]`/`mecillinam[p]` first nonzero at gen 3 local 1,611 → 8,390 + 1,611 = 10,001 (Δ +1 vs 898's −2,937); persists into gen 4 with intracellular sulfadiazine halved at division (10,895 → 5,536 → 9,806), not reset as 898 showed. 951 (Nextflow, same image): all five drug species 0 in every generation; gen 4 (cumulative 8,811–12,411, expected fire at local ≈1,189) is bit-identical to 946's (dry mass 1,212.30 fg, same bulk total) → the offset never reached the onset on the `LineageStep` path even with #771. Shared `_elapsed_after_run`; the difference must be what the rebuilt daughters carry as `global_time` under LineageStep's outer composite (a small advancing stamp is now honoured) or the inner-clock read. **Sim 955** (1×2 on 194, `LINEAGE_DEBUG_DIVISION=1` via `-process.beforeScript`) prints the booked `_gen_elapsed` per generation; robust fix to follow on #771: inner clock first, daughter stamp only if the clock did not advance. Readout tool `$T/onset_readout.py` (duckdb over S3). |
| 2026-09-10 | **ROOT CAUSE of the onset story (sim 955, `LINEAGE_DEBUG_DIVISION=1`, 08:15Z): the Nextflow path does not stop at division.** `DIVISION at t=2528s` → `[lineage-debug] t=1072.0` (= 3,600 − 2,528): the inner composite runs both daughters to the window end and the next founder is daughter `00` aged 1,072 s past division; the "daughter stamp" is that age. The chain path (1 s ticks) ends a generation at the division tick. **Corrections, stated on #166**: ~~898 fired ~2,900 s early~~ — under the Nextflow path's window semantics 7,200 + 2,801 = **10,001 s**, on time; #767's premise was wrong for that path; #771 repairs the chain path only (952: 10,001 s) and on Nextflow books daughter ages (951: 1,072 / 1,926 / 2,321 / 270 → never reaches the onset). The two dispatch paths implement different lineage semantics (ours to reconcile; which one Run 3 wants is Eran's/Chris's call). Proposed fix drafted: slice the inner run in `_run_until_division` and break on the structural change / divide flag so every path books the division time and carries the daughter at division (no sibling compute past division). Observability lesson: one `lineage.generation.end{duration}` event shows 1,072 on the first run. |
| 2026-09-10 | **Observability, cluster rung (09:08Z): first live event stream with zero configuration.** Simulator **196** = sms-ecoli `feat/events-pin` (`560f28b8`: v2ecoli `bisect/events-plus-769-771` `3726bfd4` = #772 + #769 + #771, on process-bigraph #209 `66bccbdd`; both pins must move together — sms-ecoli pins process-bigraph itself). Sims **956** (Nextflow 1×2, 898's shape) and **957** (chain 1×2). No env from viva-api (0.9.137): events on task stdout (CloudWatch) + `events.jsonl` beside the output. 956's lineage task, first 10 min: `span.start` task/lineage/generation with `baggage` {experiment_id, variant, lineage_seed} bound by the runner, `lineage.generation.start` (gen_seed 821328062, parquet/400), engine `run.start` (2 processes, 54 steps), `tick` every ~30 s wall carrying `global_time` (101 → 278 → 437 s), `lineage.chunk.flushed` at emit 400. Control = simulator 194 (951/952) for the invariance check at completion. PRs: process-bigraph#209 (engine, generalised: `component`, dotted names, string baggage), v2ecoli#772 (runner), viva-api#609 (PR-A) + #612 (PR-D, stacked); v2ecoli#773 (stop-at-division) held for the semantics call. |
| 2026-09-10 | **Observability cluster rung — acceptance (10:05Z).** Invariance, instrumented 196 vs control 194, row-by-row on `global_time`: 956 vs 951 (Nextflow, gens 0–1, 4,204 rows) and 957 vs 952 (chain, gens 0–1, 5,276 rows) — **0 differing bulk vectors, 0.0 on six mass listeners**, identical divisions. 956's `sweep_v0_s0/events.jsonl`: 94 lines (seq 3–96; the task span's first two lines are stdout-only), 0 duplicates; CloudWatch 96 JSON lines; engine `run.end` 845 / 913 s wall per generation (processes 770 / 832, engine 75 / 81, 3,600 ticks each); runner `lineage.generation.end.duration` 1,072 / 1,926 (window semantics, live). Overhead: 1,887 s vs the uninstrumented 955's 1,889 s (−0.1 %). **Gap found**: the chain path is silent — `run_pbg.py` bypasses the engine entrypoints, so the library's off-by-default applies and no sink is configured (0 JSON lines, no `events.jsonl`; `summary.json` durations 2,528 / 2,746 confirm division semantics on chain). Fix: `events.configure(default='stdout')` in `run_pbg.py` (+ PR-A's env). Tools: `$T/invariance.py`. |
| 2026-09-10 | **MERGE GATE NOT MET (13:53Z): sim 958 (Nextflow, simulator 197 = #769 + #771 + #773) never doses.** Divisions 2,528 / 2,744 / 3,144 / 3,100 / 3,129 (cumulative 14,645 s), every generation `divided=True timed_out=False` — so **#773's stop-at-division works on the cluster** and the Nextflow trajectory now tracks the chain path (gens 0–2: 958 2,528/2,744/3,144 vs chain 952 2,528/2,746/3,116; the pre-#773 Nextflow run 951 gave 2,528/1,674/1,279). But no drug species is non-zero in any generation, including gen 3 where the 10,000 s onset should fire at local 1,584. **Cause, measured by probing `_elapsed_after_run` on the #773 branch, not inferred**: on the single-window path `previous` (= `_gen_elapsed`) is always 0.0 because the whole generation is one `update()` call, and at the slice break the daughters have been alive 0–10 s, so #771's "honour a stamp only if it advances" guard lets that tiny age beat the inner clock. Probe: daughters aged 2 s with inner clock 2,530 → **2.0**; daughters aged exactly 0 → 2,530. The chain path is unaffected because `_gen_elapsed` accumulates across per-tick calls (previous 2,527 → returns 2,528), which is also why 959 reproduces 952 exactly (2,528 / 2,746 / 3,116 …) and why #773 is a no-op there. Fix = precedence swap (inner clock first, daughter stamp only when the clock did not advance) — the same fix the 951/952 readout recommended, and exactly what PR-C's `duration_vs_emits` self-check was built to catch (it would have fired on all five generations: booked ~2 s vs emits 2,529 × 1 s). Reported on sms-ecoli#166 (13:58Z) with a recommendation to hold #769/#771/#773 until a corrected image re-runs 958's shape. |
| 2026-09-10 | **MERGE GATE MET (16:09Z): sims 960 (Nextflow) and 961 (chain), simulator 198 = #769 + #771 + #773 incl. the inner-clock precedence fix (`363a2dcc`). Both dose at cumulative 10,000 s ± 1 s.** Divisions, local seconds per generation — 960: 2,528 / 2,744 / 3,144 / 3,100 / 3,112; 961: 2,528 / 2,746 / 3,116 / 2,833 / 2,626. Every generation on both paths `divided=True timed_out=False`. **Onset measured from the parquet history, not inferred**: both drugs arrive in **generation 3**, 960 at local 1,571 s (booked cumulative 8,429–8,430 → onset 10,000 ± 1) and 961 at local 1,611 s (booked cumulative 8,390 → onset **10,001**). First non-zero counts: `CPD-20940[c]` 3,125 / 3,212, `CPD-20940[p]` 1,258 / 1,419, `mecillinam[p]` 7 / 8; 961 accumulates to `[c]` 8,855 / `[p]` 2,212 by t=1,999, with `mecillinam_hydrolyzed[p]` from 1,984 and the `mecillinam[p]-EG10606-MONOMER[i]` adduct from 1,793 — periplasmic chemistry running, not just delivery. **The 40 s offset between the two paths is expected and bounded**: chain detects division per tick so its booked duration is the division time exactly (8,390); Nextflow under #773 slices the window by `division_poll_interval` (10 s) and breaks on the signal, so each generation books its division time rounded up to the slice boundary (+2, +6, +6 across gens 0–2 → 8,416 booked as 8,429–8,430). Bounded by 10 s/generation by construction. Generations 0–2 (pre-dose) agree to within 28 s and gen 0 is identical; the divergence from gen 3 is the drug. Posted to sms-ecoli#166 (comment 5621802506) with the merge order #771 → #773, #769 alongside. Evidence: `vecoli-output/sim198-run3-pilot-combo21-5g-clockfix-2a77/` and `.../-chain-5737/`. |
| 2026-09-10 | **Sequencing note raised with the gate (16:1xZ).** v2ecoli#768 (ecoli-sources finite-TRP) is approved by @cplong90 and green-lit by @eagmon (14:57Z), but merging it moves the ParCa cache identity — `inputs_hash` folds the source layer, so the `840bc973`-built caches (incl. the `6299ba5/` genotype set) no longer match a simulator built after it. A "fresh simulator from current main" for the **Run 2 J3 re-dispatch** would therefore carry the new chassis. Flagged on #166 so the order is decided deliberately rather than falling out of merge timing. Not merged by us. |
| 2026-09-10 | **Ray/multi-node path vs an instrumented image (15:50Z), after Jim asked.** Verified, not reasoned: it instruments itself with **no extra wiring** — `get_emitter()` lazily calls `configure(default='none')` which still reads `PBG_EVENT_SINKS`; the dispatcher already puts the `PBG_*` block on every node (`with_events_env` → `task_env` → `shared_env` → the single `"0:"` node-property override) and Ray workers inherit it; and inside an actor `_RayBatchActor.batch_update` → `composite.update()` → `Composite.run()` is the fully hooked path, so actors emit ticks, structural changes and process exceptions from inside themselves. **The plan's risk note "MNP actors need `PBG_*` through `runtime_env`" is retracted** (viva-api#619, plan §D5). **Two defects it exposed, fixed in v2ecoli#772 (`4d6a4e22`, 5 tests)**: (a) the S3 object key was `AWS_BATCH_JOB_ID`, which on a multi-node job is per **node** not per process, so every Ray worker on a node rewrote the same whole object and silently dropped the others' events — pid appended; (b) the sink buffer was unbounded under a whole-object rewrite (cumulative bytes ∝ event count²) — now head + rolling tail with an explicit `sink.truncated` marker. **One open engine-contract question left for @eagmon** on process-bigraph#209 (comment 5621486676): `run.start`/`run.end` fire per `Composite.run`, which on this path is one tick of one cell, so the rate is `2 × cells × ticks` (≈720k events for 100 cells over 3,600 s, vs ~360/generation on the lineage path). A depth rule does not work — in an actor there is no enclosing run; the distinguishing property is the rate. Three options on the PR; preferred is per-Composite rate limiting that always emits the first run and any run ending in error. |
| 2026-09-10 | **viva-api#618 filed (pre-existing, not ours).** `alembic upgrade head` cannot build a working schema from empty: `analysis` and the compose tables are ALTERed by migrations that never CREATE them, and several `hpcrun` columns exist only via `create_all`. Two places in the tree already shim it by hand. Surfaced by the new `tests/simulation/test_observability_migration.py` (PR #609 `baad10a3`, PR #612 `5b9b2d87`), which drives the **real** alembic chain for our own migration — the gap `create_all`-built fixtures are structurally incapable of catching, and the same class as the production `CANCELLED` enum failure. Those tests therefore shim the pre-existing gaps and scope their schema-agreement assertions to the objects our migration owns; stated on the PRs rather than hidden. Blocks the stated end goal of guarding `create_all` off in production. |
| 2026-09-10 | **Lineage stack merged to v2ecoli main by @eagmon (21:41–22:07Z), then #777 (22:24Z).** #771 `c0c06b64` → #773 `ad594719` (rebased to drop its duplicated #771 commit after the squash) → #769 `90cd0038` (one-file test-union rebase on `test_workflow_lineage.py`). Twice I rebased a PR that had gone DIRTY and Eran had already pushed the identical tree 1–2 min earlier — `--force-with-lease` correctly refused; rule recorded: when the reviewer is driving the train, do not duplicate the rebase. #777 (`96b27f38`, Eran, fail-loud when a generation's xarray emitter cannot persist — removes the try/except swallow whose silent failure was the real `_check_group` gen2/gen3 gang-teardown behind 747's and 901's deaths) merged 22:24Z. **sms-ecoli#337 moved from `90cd0038` to `96b27f38` at 22:28Z** (comment lists #771/#773/#769/#774/#775/#777; still excludes #768 on purpose — it re-keys every ParCa cache and Chris/Eran are sequencing it against the with_trp rebuild); green on all 11 checks at `490a5ab6`, OPEN, unmerged — the merge is Jim's/Eran's call. v2ecoli#768 remains OPEN/BEHIND. |
| 2026-09-10 | **The re-fire sequencing, as the three of them settled it on #166 (22:24–22:42Z) — recorded so nobody fires twice.** @cplong90 22:24Z: Run 2 J3 already went out as **962** at 18:24Z on `4bd7c82`, **pinned before #773 on purpose** so it runs the same code as 749; 749 stays as the cell-only half of Run 1; the coupled Run 1 re-fire goes on **simulator 199** (aeration just recalibrated against the current model's oxygen demand — do not move it to a post-stack image without another calibration round); fresh post-stack re-fires are additional, compared in the morning. @eagmon 22:30–22:39Z: govcloud recovered both full MNP specs from the failed Batch jobs' `describe-jobs nodeProperties` (founders live inside the existing staged caches — `_seed_overrides/0..9` — so the founder dependency is resolved); re-fire = same job, image swapped to v2ecoli `96b27f38`, new `experiment_id`; and "pin `96b27f38` or later, NOT just #769/#771/#773" — the bisect branches and every simulator up to 199 are pre-#777. My 22:42Z reply: #337 already is at `96b27f38`; the mechanism is not bespoke — `POST /api/v1/simulations` with `extra_params={"multi_node_dispatch": <db 747 / 901 block verbatim>}` plus the new `simulator_id`/`experiment_id` (dumps `$T/dispatch_747.json`, `dispatch_901.json`); **not** `atlantis composite run`, which has no `--cache-variant` and silently drops `cache_variant` from `--params` (viva-api#623, filed 22:43Z). Chain: merge #337 → build a simulator from sms-ecoli main → govcloud fires via the API. **I fire nothing.** 962: `running` at 22:47Z, gen 2→3 is the acceptance; the poller pings on terminal. |
| 2026-09-10 | **viva-api 0.9.138 on smsvpctest (20:5xZ), and PARTIAL is gone everywhere.** #621 (`aeration_trigger` hardening; my "it blocks the coupled re-fire" claim was WRONG — `run_mbp_tracked.py:807-817` `trigger = trigger or file_trigger` and the file declares `biomass`; corrected on #166) merged `9445fbda`; #622 bump `ec737ffa`; live pod `api-579877b6ff-7cn4z`, marker ×6. Trap re-hit and recorded: the first `kubectl apply -k` rendered the LOCAL checkout (still at 0.9.137) → `deployment unchanged`, green rollout, old pod; the marker grep on the NEWEST pod is the only load-bearing check. Jim's call "drop PARTIAL and keep it simple" (a run that stopped with some results stopped because something failed; the telemetry says which task): removed from `JobStatus`/`JobStatusDB`/`classify_run` (failed rows or `other` → FAILED)/chain finalize (k/N in the message)/CLI/TUI/theme/spec/client on #609 `5469a087`+`5b9d3fe6` and #612 (rebased `13f5d9d2`), with pinning tests; and from vivarium-workbench `remote_run_views.py` (`_TERMINAL_BAD = {failed, cancelled, error}`) in vwb#1047 (`078250bf`, merged) — the PARTIAL that was on workbench main had been cherry-picked from the unmerged PR-D by Alex's session, never deployed from sms-api. vwb#1047 also adds the **Nextflow dispatch mechanism to the study page** (`cp-nf-*` fields, default composite `v2ecoli.composites.workflow_nf.workflow_nf`, `cache_variant` refused, no `../v2ecoli` dependency). Migration `a3b5c7d9e1f2` is now pure addition (10 nullable `hpcrun` columns + `hpcrun_event`/`hpcrun_span`); rollback = roll the image back, and do not re-run the OLD migration Job afterwards without `alembic stamp f76e43d01841` first. |
| 2026-09-10 | **Observability PRs put in front of Eran and Alex (21:52Z, #166 5625929340), with one correction (22:01Z).** Jim asked for a benefits/risks brief with verified claims: instrumented simulators get `atlantis simulation status/events/tasks` without AWS credentials; non-instrumented ones keep running unchanged (the ingester reads an empty prefix; old rows gain only NULL columns); rollback-safe (old app never reads `alembic_version`). The correction, prompted by Jim's "what is a scheduler tick": it is **sms-api's `JobScheduler._polling_loop`, hardcoded 5 s (`main.py:99`)**, not process-bigraph's 30 s debug heartbeat; and the "bounded by `events_ingest_max_objects_per_tick`" claim was **false** for a run that never emits — it reads 0 objects and the 600 s idle gate is `last is None or …`, so such a run costs one serial S3 LIST per 5 s until terminal + 900 s grace. Fix (2 lines in `is_ingest_candidate`: idle-skip when `last is None and now - start_time > idle_seconds`) offered, **not pushed** pending their review; likewise the split-vs-combine question for #609/#612. Held for Jim's review, uncommitted, never pushed: the tolerant-status-read work (`$T/wt-tolerant`, `UNRECOGNIZED` sentinel via a mixin on both `sa.Enum` and `postgresql.ENUM`). |
| 2026-09-10 | **Jim: "do both" (23:00Z) → #337 merged, simulator 200 built, both post-stack re-fires fired via the standard API (23:12–23:13Z).** sms-ecoli#337 squash-merged 23:01Z → main `ddc1a83b` (v2ecoli `96b27f38`). Simulator **200** = `ddc1a83`, built 23:01→23:11Z. The trap and its fix: the MNP path's variant-cache guard derives `cache_s3_uri(simulator.git_commit_hash, cache_variant)` and refuses an empty slot, so the two chassis caches were copied server-side (`aws s3 cp --recursive`, 6 s) into `ray-parca-cache/ddc1a83/`: J3 from `6299ba5/cd2-run2-j3-candidate-v1-lambda075/` (6 base objects, `_seed_overrides/*` excluded — the dispatcher re-stages the ten `2fddfcb8…/…-founder-seedN` caches itself) and K4 from `17c3f9c/cd2-run1-k4-candidate-v1-lambda050/` (6 objects). Then `POST /api/v1/simulations?simulator_id=200&simulation_config_filename=mecillinam_wellmixed.json&experiment_id=…&tags=…` with `{"extra_params": {"multi_node_dispatch": <db 747 / 901 block verbatim>}}` plus `task_env: {V2ECOLI_SKIP_CACHE_VERIFY: "1"}` on both (the copied `cache_version.json` records its build commit; 901 and 962 already ran that way) and a fresh `params.experiment_id` on the 901 clone (`run_pbg.py:803`: an explicit override key wins over `--experiment-id`). Accepted: **963** `sim200-cd2-run2-j3-refire-96b27f38-10x10-3e86` (Batch `864caa52-…`), **964** `sim200-cd2-run1-k4-cellonly-refire4-gen10-96b27f38-42e3` (Batch `e4160574-…`), both RUNNABLE at 23:14Z, founders staged 0..9, no ParCa job (caches found). Additional to 962 and to the coupled run on 199, per Chris; posted on #166 (5626650921, 5626702890). Note in passing: `ray-parca-cache/4a5b23f/` (simulator 199's slot) was still EMPTY at 23:04Z, so the coupled re-fire's "founder copy" had not landed by then. Gotcha re-hit: a zsh `for spec in "a b c"; set -- $spec` loop does not word-split — the first attempt sent nothing (curl failed before connecting); use a bash script file. |
| 2026-09-10 | **23:15–23:30Z: three asks on #166, and a wrong "962 is hung".** (1) Alex: copy the K4 founders `2986cf5/…-founder-seed0..9` → simulator 199's slot; done into `4a5b23f/` (60 objects; 7-char key = the API's `git_commit_hash`, not his "4a5b23fb"); the chassis slot `4a5b23f/cd2-run1-k4-candidate-v1-lambda050/` deliberately NOT guessed (Dispatch 339/340 class) — asked for the source. (2) Eran: "who fires the Run 3 full sweep?" — Alex claimed it at 23:21Z (36 MNP dispatches on simulator 200; the 09-09 chain sweep on 186 never dosed by construction); the Nextflow shape (`--run3-sweep`) stays a hedge, Jim's call. (3) Chris: 964 (901's block, one shared cache) would start all ten lineages from the same founder — cancelled 964 before its gang placed and re-fired as **965** `sim200-cd2-run1-k4-cellonly-refire5-founders-gen10-96b27f38-7053` with `seed_overrides` → the `6299ba534dbdd1cf…/…-founder-seed{N}` caches 749 ran from; founders staged 0..9, Batch RUNNABLE. Live set: **962, 963, 965**. Then Eran (23:20Z) declared 962 "dead weight — zero output, no Batch child jobs". **Checked, false**: 20 child jobs on `smsvpctest-vecoli-task-amd64` (10 parca SUCCEEDED 18:23Z; 10 lineages RUNNABLE 18:27Z → placed 20:47Z), ~40 min/generation, at 23:28Z all ten past gen 2→3 and two at gen 4 (v6 gen 4 `divided=False timed_out=True`). The results prefix is empty because Nextflow lineage tasks upload outputs and `.command.log` at task END (chain syncs every 30 s) — presence-vs-effect in the other direction, and precisely the gap #609/#612 close. Corrected on #166 with the per-lineage markers. |
| 2026-09-11 | **00:07–00:56Z: the Run 4 minimal tail — a wrong "ceiling", a real science call, and 42 re-fires.** Eran (00:07Z): genotypes 13–22 "plateaued at 28/32 = the final-gen `_check_group` teardown; who re-fires the 30-genotype tail?" Checked first (his 962 call an hour earlier had been wrong): all 42 rows (db 900–942) = 12 completed at 32/32 with `final_state.json`, 30 running; a sweep of every results prefix (`$T/run4_tail_sweep.py`) showed g13 at 30/32 and g14 at 31/32 **with generation-7 markers already landed**, every one of the 30 written to S3 within 50 min (19 within 5), g37–42 still in generation 0 — an unfinished 8-generation campaign, not a teardown. Ray-path lesson: the head node's CloudWatch stream (`smsvpctest-ray-batch-RayBatchLogs…` group, not `/aws/batch/job`) goes quiet after "cluster formed"; mid-run truth is the results prefix's periodic sync (`success/` count, newest-object age; `aws s3 ls` prints local time). Also found: those 30 gangs WERE the whole `smsvpctest-ray-mnp` queue (30 RUNNING / 2 RUNNABLE = 963 + 965). Then Chris (00:29Z, 00:39Z): the whole campaign ran pre-sms-ecoli#301 (metabolism_redux, zero the 51 BAD_RXNS, merged 09-09; `b3b4a628` behind, `ddc1a83` ahead) — **"I want the full re-run, all 42 on post-#301, for the delivery."** Jim: "give chris the run he is looking for", and the new standing rule — *on what simulations to run, Chris or Eran decide; I execute unless I disagree on pipeline grounds* (memory `science_decisions_chris_eran`). Done 00:51–00:56Z: 42 caches copied `b3b4a628…/cd2-run4-carina-genotypeN/` → `ddc1a83/` (42/42 × 6 objects), then 42 × `POST /simulations` with each row's block verbatim + skip-verify (`$T/run4_fire_sim200.py`) → **db 1039–1082** (g N → 1038+N for N ≤ 39; g40 = 1079, g41 = 1080, g42 = 1082; 1078 = Alex's coupled Run 1 seed 1 on 199), 0 ParCa jobs, 42 RUNNABLE behind the 29 remaining pre-#301 gangs and 963/965. Cancelling those 29 (superseded output) offered as Chris's/Eran's call. Alex's Run 3 sweep (1002–1037, 00:48Z) is chain-dispatch, no contention. |
| 2026-09-11 | **01:09–01:20Z: 963 FAILED at the Ray entrypoint's cache pre-check — `V2ECOLI_SKIP_CACHE_VERIFY` was never honored there.** Node 0, 48 s after placing: `FATAL: staged cache … stale / version-mismatched … files differ: ['v2ecoli/composites/reactor_bird_coupled.py']` (J3 chassis built at `6299ba5`, before v2ecoli#758). `docker/ray-batch-entrypoint.sh` calls the raw `verify_cache_version()`; the flag is read only by `v2ecoli/core.py`'s wrapper, which runs later — so on the MNP path a copied/re-keyed cache could never pass, and the escape hatch had only ever been exercised on Nextflow (`beforeScript` export → core.py's check). 965 (K4 cache built at `17c3f9c`, post-#758) passed and is running, which isolated the cause. Fix: **sms-ecoli#339** (WARN + continue when the flag is set; FATAL otherwise). Cancelled all 42 Run 4 post-#301 re-fires (1039–1082) before they placed — one was already `pending` — API cancel plus `aws batch terminate-job` (the API cancel alone left them RUNNABLE in Batch for ~20 s). Plan: merge #339 → simulator 201 (same v2ecoli `96b27f38`) → copy the J3 chassis + 42 genotype caches into 201's slot (`$T/refire_on_new_sim.sh <commit> <sim> --fire`) → re-fire 963's block + the 42. v2ecoli#768 (rebased `0c6e2fadc`, CI running) stays a separate later pin with Eran's #338 → simulator 202 for the with_trp arm (chassis via a `cache_variant` slot + 42 inductions with `source_variant`; design verified against the existing caches: expression 1,174,897.55, trl_eff 0.285, 1-based genotype order). Also: Eran's SSO-expiry question (01:05Z) unanswered yet; github-sync cron moved to every 5 min. |
| 2026-09-11 | **01:19–01:40Z: #339 merged (`15aba29`), simulator 201 built, re-fires out; #768 merged; #340 opened.** sms-ecoli#339 squash-merged 01:19Z (11/11), simulator **201** = `15aba29` (same v2ecoli `96b27f38`; also carries Eran's #338 vendor bump) built 01:19→01:30Z. Caches copied into `15aba29/` (J3 chassis 6/6, 42 genotypes 42/42; `$T/refire_on_new_sim.sh`). Re-fired: **1088** = Run 2 J3 (`sim201-cd2-run2-j3-refire2-96b27f38-10x10-7924`, 963's block verbatim) and the **42 Run 4 minimal post-#301 genotypes** (label `…-post301-96b27f38-sim201`), all with zero ParCa jobs (copied caches found), all RUNNABLE behind the pre-#301 tail. Chris (01:36Z): prefers 201 as done — keeps J3 on the same v2ecoli as 965's K4 run, a comparable pair. Eran (01:30Z): v2ecoli#778 makes the library verifier honor the flag (complements #339); merging on green. **v2ecoli#768 merged 01:19Z (merge commit `8b221c5f`)** → sms-ecoli#340 (`chore/pin-v2ecoli-8b221c5f`) opened for the with_trp arm: first CI failed because sms-ecoli pins ecoli-sources DIRECTLY as well (`pyproject.toml:298`) — a v2ecoli bump that moves ecoli-sources needs three edits (v2ecoli rev, own ecoli-sources rev, uv.lock ×2) or uv re-resolves and CI dies cloning the private `sms-goodness-of-fit`; fixed (`19caf2af`, `uv lock --check` clean, 11/11). #340 held unmerged to be re-pointed at the post-#778 merge commit so simulator 202 carries #768 + #778 in one bump. Also answered Eran's SSO-expiry question (01:27Z): server side is IRSA (`BatchSubmitIrsaRole`), laptop side is Identity Center with two clocks (permission-set 1–12 h, session up to 90 d) — fixes: `[sso-session]` layout + longer session duration (Identity Center admin, not sms-cdk), and move the flush in-cluster as a Job. |
| 2026-09-11 | **01:51–02:10Z: cancel of the pre-#301 tail, the redux swap proven on the wire, #339 proven live, and an account-level ceiling.** Chris (01:51Z) asked to cancel the remaining pre-#301 Run 4 gangs → 22 cancelled (API + `terminate-job`; the API cancel alone leaves MNP gangs RUNNING in Batch for ~1 min). Chris's worry that the swap was dropped (#320 class): the stored `multi_node_dispatch.params` carries no `injected_processes`, but the dispatcher threads the config's top-level `swap_processes` into the composite overrides at submit (`_thread_injected_processes_into_params`, `simulation_service_ray.py:3355`); proof = `RAY_JOB_CMD` on 1089's Batch job spec: `--overrides '{"injected_processes": {"swap_processes": {"ecoli-metabolism": "ecoli-metabolism-redux"}, …}, "media": "minimal"}'`. **Check the wire, not the row.** 01:59Z: `1088` placed and its node-0 log shows the #339 WARN-and-continue on the exact line that killed 963. Then the with_trp simulator (202, `47ea00f`) would not build: `v2ecoli-ray-build-47ea00f` RUNNABLE since 01:39Z, ASG activity `VcpuLimitExceeded … limit of 2500` — the account quota L-1216C47A (2,500 on-demand Standard vCPU); usage 2,624 = RayBatchOnDemandCE 1,760 (MNP gangs) + 832 untagged Batch (chain/task) + 32 misc. Nothing new launches until gangs exit; sms-cdk#51's 2,048 raise is capped by it in practice. Options posted (wait, or a quota increase to ~4,000 — asked Jim before filing). Also: Eran re-pinged for a "build-once" Run 3 re-fire → answered no (Alex's 1002–1037 live; chain path has no such knob; `run_parca=false` → 400; `parca_dataset_id` is a placeholder). |
| 2026-09-11 | **02:25Z: 962 is being Spot-reclaimed; correction of the 23:29Z "queued 2 h 20 min".** Batch `attempts[]`: all ten lineage tasks lost attempt 1 at 20:43Z (`Host EC2 … terminated`), v4/v6/v7 lost attempt 2 at 23:35Z, v3/v8/v9 at 00:08Z; each reclaim restarts the lineage at generation 0 because `workflow_nf`'s `checkpoint_dir` is task-local. The task queue is Spot-first (`Amd64SpotComputeEnv`, 2,000 vCPU) with a 256-vCPU on-demand fallback; the multi-node Ray CE is on-demand, so 965/1088/the 42 are not exposed. Morning pick per the evidence: `1088` + `965` (post-#777, on-demand) are the dependable Run 2/Run 1 pair; `962` is the hedge. Backlog: route the `lineage` label to on-demand (per-label `queue` / `max_spot_attempts`) and/or S3-synced per-generation checkpoint with resume across attempts. Rule: read `attempts[]` before calling a Batch job "queued". |
| 2026-09-11 | **02:26–03:09Z: 965's emitter failure diagnosed from the per-node Ray logs, J3 moved to parquet, quota raised, with_trp stage 1 out.** 965 died at its generation 1→2 boundary on seed 7: `_check_group` "Missing path from previous generation … emitstep_gen=1" — the #777-loud form of the 747/901 gang teardown. Per-node logs (`ray-logs/<job>/node-N.tgz`): EVERY seed printed `gen 0: emitters flushed in 0.0s`; exactly one 4-object skeleton `.zarr` per run reaches S3 (the raising seed's); no "no leaves / skipping xarray" warning anywhere. Mechanism that fits: viva-emitters 0.4.0 `XArrayEmitter.flush()` returns silently when `writer._buffer is None` and `advance_generation`/`close` skip `mark_success`/`close` on the same condition → finalize is a no-op → next open fails; and `_assert_generation_emitted`'s xarray zero-emit check is an `elif` that `emitter: both` never reaches. Filed v2ecoli#779; Eran's #782 (diagnostic guards) commented with both facts; his "durability across the gang" hypothesis refuted (store is node-local, same actor/process). Chris: cancel 1088, re-fire J3 on parquet → **1131** (`sim201-cd2-run2-j3-refire3-parquet-96b27f38-10x10-dd03`). 962 revealed as Spot-reclaimed ×3 (viva-api#624). Jim OK'd the Service Quotas raise on L-1216C47A 2,500→4,000 (filed 02:40Z, applied 02:55Z, case 178909443106630); simulator **202** (`47ea00f`, #768 + #339 + #338) built 02:59→03:08Z; with_trp stage 1 = chassis dispatch **1132** (parca_dataset 264, `cache_variant cd2-run4-chassis-post768`, ParCa `e496fb4e…`). Eran corrected on the stale "re-fire the 30 Run 4 gangs" (they were cancelled and re-fired on 201 hours earlier). |
| 2026-09-11 | **04:31–04:50Z: RUN 3 DELIVERABLE (Alex's chain sweep on simulator 200, Eran's readout).** Dose delivered at 10,000 s cumulative on a production chain run — the #771/#773 clock fix working for real; wire check on combo21's chain job (`CONTAINER_JOB_CMD --overrides`) shows the full declared injection (9 add_processes incl. `sulfadiazine-dhps-inhibition`, all process_configs) — the #320 drop class ruled out. Dose-response monotone and mechanistic: combo21 (sulfa 1e-2) −5.7% DHPS flux, growth flat (sub-inhibitory); combo05 (sulfa 1, sim 1007) −41% DHPS, −32% growth at gens 4–6. Caveat raised: combo05 "recovers" at the gen 6→7 division with CPD-20940 still ~1M; Eran reads it as single-daughter sampling, but the inhibition process is stateless (bound recomputed per tick from bulk counts; silent `{}` if the species index fails to resolve in a fresh container), so a boundary-aligned jump is also the signature of the bound not being written/applied in gen 7's container — check posted (recompute the bound from gen 7's first-tick parquet vs observed flux). Also: Alex filed v2ecoli#784 (equilibrium solver crash = 1094 / old g24); account back at 4,000/4,000 after the Ray pool absorbed the headroom (30 Run 4 gangs running); 1131 + the with_trp chassis still RUNNABLE behind the FIFO; quota 4,000→5,000 decision with Jim. |
| 2026-09-11 | **05:10Z snapshot.** 962: 7/10 lineages complete (all ten generations), 3 at gen 7–8 on attempt 2, no Spot reclaim since 00:11Z, ETA 06:00–06:30Z; its gather predates #760 so the API will say `failed` with complete lineage outputs. Run 4 ×42 on 201 (parquet): 30 running, first wave at generation 4 of 8, 11 queued, one solver failure (g6/1094, v2ecoli#784). 1131 and the with_trp chassis still RUNNABLE; account at its 4,000 quota (Ray 1,920 + standalone 2,048 + misc). |
| 2026-09-11 | **06:06Z: 962 complete as science, `failed` as status — as predicted.** All ten lineage tasks SUCCEEDED (seed 3 last, ~06:05Z, after three Spot reclaims); `vecoli-output/sim184-run2-j3-lambda075-refire-59d3/` holds 10 variants × generations 0–9 (1,746 objects, 34 GB). The head's `analysis_v5` failed four times (the pre-#760 gather on `4bd7c82`) → Nextflow exit non-zero → API `failed: Job has reached the specified backoff limit`. Morning pick: a complete pre-stack J3 to set against 1131 (post-#777, parquet) once that places. |
| 2026-09-11 | **06:33–07:30Z: Run 4 post-#301 completions begin; with_trp stages 1–2.** First post-#301 genotype complete at 06:33Z (g5, 32/32, 4.5 h wall vs 11–15 h pre-#301 — flagged to Chris as an observation); 12 complete by 07:16Z; second equilibrium-solver failure (g38 = db 1125, 14 min in; v2ecoli#784, and note the id map: g N → 1088+N for N ≤ 25, 1087+N for N ≥ 27, g26 = 1130). 962's last lineage finished 06:05Z (all ten × ten generations, 34 GB; API `failed` on the pre-#760 `analysis_v5`). The with_trp chassis ParCa placed at 07:14Z once a node freed (Batch slotted the 1-node job ahead of 10-node 1131), SUCCEEDED 07:26Z; slot provenance verified (violacein chassis, full mode); 42 inductions submitted 07:29Z with `source_variant=cd2-run4-chassis-post768`. Stage 3 next. |
| 2026-09-11 | **07:41–08:27Z: 1131 placed; with_trp stage 3 fired.** 1131 (Run 2 J3, ten founders, `emitter: parquet`, simulator 201) staged the copied J3 chassis, passed the pre-check with the #339 WARN, gang of 10 formed 07:42Z; acceptance = the gen 2→3 boundary (~09:00–09:30Z). The 42 with_trp inductions drained 08:10–08:22Z once the Run 3 chains freed the standalone pool — each ~22 s wall (12 s compute): `build_new_gene_cache.py` derives the new-gene bundle from the staged chassis ParCa state, it is not a ParCa; every `47ea00f/cd2-run4-carina-genotype<N>/` slot has 6 objects incl. `new_genes.json`, log values as designed (expression ×1174897.55, trl-eff 0.285). Stage 3: g1 fired first (db 1139), its Batch env verified on the wire (`RAY_JOB_CMD` overrides carry `media: minimal_plus_tryptophan`, `emitter: parquet`, the `ecoli-metabolism → ecoli-metabolism-redux` swap threaded from the config top level, `exclude_processes: [exchange_data]`; `RAY_STAGE_S3` = the genotype-1 slot at 47ea00f), then g2–42 released: **db = 1138+N (1139…1180)** on simulator 202, tags `cd2-run4` + `withtrp-post768`. Posted #166 (5631679854). Run 4 minimal on 201 at that moment: 24 complete / 16 running / 2 solver failures. |
| 2026-09-11 | **08:41Z: with_trp g6 (db 1144) fails at 24 min with the same `equilibrium.py:435` "Negative values at equilibrium steady state" as minimal g6 (1094, 89 min) and g38 (1125, 14 min).** Two images (#301-era / #768), two media, same genotype → the genotype-6 inputs to the legacy solver, not the pipeline; added to v2ecoli#784. Neither 6 nor 38 re-fired pending the solver owner. with_trp at 08:55Z: 13 running, 1 failed, 28 queued; Run 4 minimal on 201: 25 complete / 15 running / 2 failed. |
| 2026-09-11 | **09:07–09:40Z: Run 4 post-run analyses are OOM-killed.** 11 of the first 23 `ray-mnp-analysis-sim201-*` jobs (16 vCPU / 60,000 MB = the whole m5.4xlarge) exited rc=137 after 55–90 min; 12 still running. The single scale completes and is synced (128 per-cell ptools TSVs, 256 viz, `analysis.json` 91.8 MB `status: PARTIAL`, its `runtime` block shows peak RSS reaching 32.4 GB by the end of single); the kernel kills the process in the multigeneration/multiseed phase (missing vs sim166: `ptools_*_multiseed`, `replication`, `ribosome_*`). Same shape succeeded on sim166 / v2ecoli 2e54e95a in 93 min with a larger history → regression between the pins: #770 (multiseed ptools `.df()` full materialisation incl. list columns + Python collapse) and #671 (ptools_overview at multi scales), on a runner defaulting to 10 concurrent modules with no DuckDB memory_limit. Simulations unaffected; API says `completed` (1093) — the analysis failure is invisible. Filed v2ecoli#786 and viva-api#625 (silent status + `submit_multi_node_analysis` does not forward `task_env`, so `V2E_ANALYSIS_MAX_WORKERS`/`V2E_ANALYSIS_MEMORY_LIMIT` cannot be set). Posted #166 (5632266613) with the decision for Chris/Eran: let the 12 run out or stop them; re-run analyses later with workers=1 / restricted modules after (a) or (b). Not rolling the api mid-campaign without a nod. |
| 2026-09-11 | **09:45Z: 1131 ACCEPTED past the xarray-killing boundaries.** All ten founders hold `success/` markers for generations 0, 1, 2 (gen 2 closed ~09:40Z), gang alive, fresh output on S3; 965 died at 1→2 and 747 at 2→3 on xarray (v2ecoli#779) — on parquet the same chassis/founders pass. ~40 min/generation → gen 9 ≈ 14:30Z. Posted #166. Its post-run 10×10 analysis will hit the v2ecoli#786 OOM under current code. Run 4 minimal: 28/12/2; with_trp: 16 running, 25 queued, 1 failed (g6); analyses: 19 failed / 7 running / 0 succeeded. |
| 2026-09-11 | **12:05Z: sms-ecoli#345 merged (46bff06d).** The Run 3 dose-response outcome is folded into `cd2/investigation.yaml` with the combo05 gen-7 uptick recorded as "single-daughter lineage-sampling artifact, NOT resistance" — as settled, not as the open question I asked for at 04:54Z (the stateless DHPS bound may simply not have been applied in the gen-7 container; distinguishing check = recompute the bound from gen-7 bulk counts/mass and compare with the emitted H2PTEROATESYNTH flux). Science record is Eran's; the check stays on our list. v2ecoli#782 (xarray open guard) still open with merge-main only; the four cd3-hardening test PRs likewise; workbench #1048/#1049 merged. |
| 2026-09-11 | **12:18–12:27Z: Eran's two decisions; Run 1 cell-only re-fired on parquet.** (a) Run 4 analysis OOM: let the running analyses finish, recover multiseed/multigeneration products as a batched re-run with `V2E_ANALYSIS_MAX_WORKERS=1` once the knob lands (viva-api#625 / v2ecoli#786), no api roll mid-campaign. (b) Gang runs go on `emitter: parquet` (1131 proved it); confirmed with_trp 42 and Run 4 on 201 are parquet already. **1181** = 965's block (ten founders, K4 lambda-0.50 chassis, 10×10, redux swap, skip-verify) with the emitter switched to parquet and `params.experiment_id` removed, on simulator 201 (`96b27f38`, same as 965/1131); lambda050 slot copied `6299ba5/ → 15aba29/`; Batch gang `35cec7ca…` RUNNABLE 12:27Z; wire check passed (`RAY_JOB_CMD` emitter parquet, `--experiment-id` = API's, `RAY_STAGE_S3` = 201 slot, `_seed_overrides/0…9` staged). Posted #166 (5634405749 + ids post). |
| 2026-09-11 | **12:27Z: v2ecoli#782 merged (45187fdc)** — the xarray open-path guard, i.e. the proper fix for the gang boundary failure that killed 747/965 (v2ecoli#777/#779). Off the critical path: every gang in flight (1131, 1181, Run 4, with_trp) is on parquet. Not in any built simulator yet (201 = `96b27f38`, 202 = `8b221c5f`); picks up on the next sms-ecoli pin bump. sms-ecoli#343 (P1 regression tests) merged in the same minute. |
| 2026-09-11 | **12:36Z: Run 4 minimal on simulator 201 complete as simulations — 40/42** (g6 = 1094 and g38 = 1125 lost to the equilibrium solver, v2ecoli#784). Placed ~03:40Z, last gang 12:36Z (~9 h at up to 30 concurrent gangs); per-gang wall time n=40 mean=5.0h min=4.4h max=5.4h. Every prefix: parquet history, 32/32 `success/` markers, single-scale ptools/viz; multiseed products pending the batched analysis re-run. Posted #166. |
| 2026-09-11 | **12:39–12:52Z: Gate-A diagnostic image built on request (Eran, non-urgent).** sms-ecoli branch `debug/gate-a-emitters-instrumentation` (`3f68541` = main `b8c071e4` + one `[tool.uv.sources]` line pinning viva-emitters to `debug/lineage-gang-instrumentation` @ `0a948d7b`, LINEAGE_DEBUG-gated, OFF ≡ v0.4.0; lock diff = that one source) → **simulator 203**, COMPLETED 12:52Z. Disposable, never merges; production simulators keep PyPI viva-emitters 0.4.0. Diagnostic gang (1 seed, ≥6 gens, `task_env LINEAGE_DEBUG=1`, xarray emitter, cache slot at `3f68541/`) is Eran's to greenlight. Also: g39's inline analysis survived at 57.3 GB peak (1/30), noted on v2ecoli#786. |
| 2026-09-11 | **12:56Z: RUN 2 DELIVERED — Chris accepted 962** (Nextflow, J3 cell-only, ten founders × ten generations, simulator 184 / v2ecoli `4bd7c82`, 1,746 objects / ~34 GB). Graded PASS on all three 2026-09-09 criteria: right design (ten distinct founders by gen-0 `cell_mass`, J3 applied == requested × 0.285 on all 11 manifests, λ0.75 chassis), readouts legible (violacein non-zero on 99.93 % of 283,683 rows; transcriptome 4,351 / proteome 4,315 per cell), KPIs in window vs 683 (vio 0.1257 vs 0.1169 mmol/gDCW/h, τ 65.7 vs 66.3 min; glucose uptake ×1.24 higher = the #301 metabolism change at generation 0, "not a deployment defect", mechanism unpinned by the scientists). The API's `failed` is the pre-#760 `analysis_v5` gather only. Second Nextflow deliverable after 683 (Run 2 earlier shape). Same minute: Alex corrected the board — Run 1 coupled 883–892 (sim188) is terminal but invalid (pre-#332 boundary bug); the live Run 1 coupled attempt is the simulator-199 kLa350 re-fire, seed 0 past gen 9/16. |
| 2026-09-11 | **15:02Z: 1131 COMPLETE — Run 2 J3 on the MNP path, parquet.** Ten founders × ten generations, 100/100 `success/` markers, `final_state.json`, 1,273 objects / 40.1 GB; gang 7.31 h wall from 07:41Z (~40 min/generation). Same chassis/founders that died on xarray at 1→2 (965) and 2→3 (747) — parquet is the working gang path end to end (Eran's decision (b) confirmed). Analysis job started; will hit v2ecoli#786 at 10×10. Posted #166. with_trp 18/22/2; 1181 in generation 1. |
| 2026-09-11 | **15:29Z: 1181 clears the gen 1→2 seam.** All ten founders hold `success/` markers for generations 0 and 1 — the seam where 965 died on seed 7 (xarray `_check_group`, v2ecoli#779) — on the same block, founders and v2ecoli pin, emitter the only change. gen 0 closed 14:49Z, gen 1 at 15:29Z (~40 min/generation, as 1131); ten generations lands ~21:30Z. Both gang arms are now clear of Gate A on parquet. Posted #166 (5636777696). |
| 2026-09-11 | **19:30Z: Run 4 with_trp arm complete — 40/42** on simulator 202 (`8b221c5f`), fired 08:27Z, last gang 19:30Z (~11 h; per-gang n=40 mean=5.0h min=4.6h max=5.5h). Verified on S3: 32/32 `success/` markers + `final_state.json` on each of the 40. Missing = g6 (1144) and g38 (1176), the same equilibrium-solver pair as the minimal arm, zero markers, v2ecoli#784, not re-fired. Both Run 4 arms therefore stand at 40/42 with the same two genotypes. Their post-run analyses hit v2ecoli#786; the proven 200 GB route can recover the multiseed set on request. |
| 2026-09-11 | **20:17–20:31Z: the +Trp arms never imported tryptophan; 16 native +Trp gangs cancelled.** Chris found violacein flat with vs without Trp (0/40 significant, median ×1.01, against carina's 38/42 at ×1.59); verified here on three history files — `sim201` minimal, `sim202` with_trp and `sim202` native-with_trp all carry the SAME 53 `estimated_exchange_dmdt` columns with no `TRP[p]`. Cause: both configs (`pathway_expression_carina_final.json`, `fss_pathway_oe_native_oe_carina.json`) declare `exclude_processes: ["exchange_data"]` + the redux swap and **no `media_id`**, so `inject.py` seeds the uptake set from the cache's condition (`minimal`); the dispatch-level `media` only reaches `environment.media_id`. Not a dispatch defect. Chris called the cancel at 20:24Z: 16 native +Trp gangs (db odd 1183–1213) cancelled and verified gone from Batch; native minimal half (16) runs on. Recommended fix = thread the run's media into the redux `media_id` before the uptake seed (Chris's option 2), not re-enabling `exchange_data`. Re-fire scope after the fix: 42 pathway +Trp + 16 native +Trp, caches reused. |
| 2026-09-11 | **21:26Z: 1181 COMPLETE — Run 1 K4 cell-only, ten founders × ten generations, parquet.** 100/100 `success/` markers, `final_state.json`, 1,274 objects / 39.4 GB, gang 7.23 h from 12:26Z (simulator 201, tag `cd2-simulator-201`). 965's block with the emitter as the only change: it died at seed 7's gen 1→2, this went the distance, as 1131 did for J3. Both gang arms clean ⇒ Gate A behind us on the gang path; v2ecoli#782 stays the proper durability fix, off the critical path. Chris opened sms-ecoli#349 for the +Trp uptake wiring and is taking the fix; the 58 +Trp gangs (42 pathway + 16 native) re-fire off existing caches once it merges. |
| 2026-09-11 | **21:44–23:30Z: the media fix shipped, 58 +Trp gangs re-fired on simulator 204, and the engine-pin question got answered on the record.** sms-ecoli#350 merged at 21:44Z (`42b1eac2`); simulator **204** built at 21:56Z (`42b1eac2` → v2ecoli `136f6af5` → ecoli-sources `86607c4d`). Pre-flight on a 1×2 pilot passed all four of Chris's criteria: **gen 0 and gen 1** of db 1218 carry **54** `estimated_exchange_dmdt` columns including `TRP[p]` (gen 1: n=400, mean +7,965, max +2.612e5; positive = uptake, `GLC[p]` +5.907e5 alongside), the `[inject] … run medium 'minimal_plus_tryptophan' != bundle medium 'minimal'; re-derived …` line is present in the node tarball (it never reaches the head stream on the MNP path), and the minimal control db 1219 has **53** columns with **zero** re-derive lines — so the gate is a no-op on minimal and the completed minimal arm is not called into question. Fired 58 gangs at ~22:48–22:55Z (db 1220–1277 = 42 pathway genotypes + 16 native designs). **Two POSTs returned no database id** (native design 10, genotype 42) and were caught only by reading the roster back from the API — standing practice now, never trust the POST response. **Process note:** the question of building the fix on `8b221c5f` (sim 201's engine, the arm this is compared against) vs `main`/`136f6af5` was raised twice by me and never answered before dispatch; I proceeded on my stated default and Alex put that on the record at 23:06Z. Evidence posted at 23:10Z: the whole delta is **v2ecoli#783 alone** (`card_vectors.py` +50 −7 and a new test), analysis-side keying, not imported by the sim path and not in the ParCa `inputs_hash` list, so no cache re-key and no trajectory change. **eagmon ratified at 23:30Z: proceed as is, no rebuild**, and asked for a non-media 201-vs-204 cross-check as confirmation, explicitly not a blocker. The lesson recorded, since it cost nothing only by luck: flagging twice is not persuading — make the case to the named people, or set a visible deadline on the objection window, *before* committing compute |
| 2026-09-12 | **01:21Z: the native-gene OE minimal arm is complete, 16/16, verified against S3.** 16 designs × 4 seeds × 8 generations on simulator 201 (db 1182,1184,…,1212): every experiment has **8 of 8** generations present, 231–281 history objects each, no failures and no partials. Its +Trp half (db odd 1183–1213) is absent by design — those were the 16 cancelled at 20:24Z for the uptake-wiring bug and re-fired inside the 58 on simulator 204, so the native comparison closes when those land. The 58 at 01:21Z: 32 running, 26 queued, none finished, none failed; first completions expected ~04:00Z at the ~7 h per-gang wall time. Run 4's auto-fired post-run analysis jobs continue to drain to failure (92 failed / 4 succeeded, the four being the small pre-flight runs) — expected, since the real analyses for this campaign run as the separately resourced 200 GB re-gather, not as the default post-run job |
| 2026-09-12 | **02:10–02:20Z: the first failure in the 58 is a dead Ray node, not the solver bug.** Alex flagged db 1242 (`genotype22-withtrp`) at Batch level as a possible third instance of v2ecoli#784. Traceback pulled from CloudWatch says otherwise: `(raylet) node 10.99.44.35 marked dead — missed too many heartbeats` → `ray.exceptions.ActorDiedError` raised out of `composite.run` → `_flush_protocol_runtimes` → `protocols/ray.py:650 ray.get(futures)`, with **no** `equilibrium` / `fluxes_and_molecules_to_SS` frame anywhere. Three facts separate it from genotypes 6 and 38: it wrote **complete history for generations 0–4** and was into generation 5 (those two die inside generation 0 at ~14 min); the dead node's own tarball has **no raylet output after 23:13Z**, i.e. the raylet stopped logging rather than logging an error, which is a host-level kill; and `smsvpctest-ray-mnp` is `type=EC2` on-demand m5.4xlarge, so **spot reclaim is ruled out**. Remaining candidates are host OOM or a full root disk (cf. the 30 GB task-queue roots, sms-cdk#48) — not established, and nothing genotype-specific should be read into it. **Re-fired as db 1278**: infrastructural cause, inside the already-sanctioned 58, so no new decision implied; roster held at 58 (32 running / 26 queued). **Tooling, because Alex could not resolve the stream:** for an MNP job the log stream is on the *attempt* (`jobs[0].attempts[].container.logStreamName`) and the group on the node ranges (`nodeProperties.nodeRangeProperties[].container.logConfiguration`), **not** `/aws/batch/job`; the `ray-logs/<job>/node-N.tgz` tarballs carry only Ray session logs and never the Python traceback. Separately, Run 4's auto post-run analysis drain finished at 02:17Z: 94 failed, 4 succeeded (the small pre-flights), 0 running — expected, the real analyses run as the resourced re-gather |
| 2026-09-12 | **THE +Trp RE-FIRE IS COMPLETE: 58/58, verified, 1,856 cells, zero defects (09:04Z, ~10 h after dispatch).** Checked per gang against S3 rather than the API's status — every one has 4 seeds × 8 generations = **32/32 cells**, 32 success markers, **54** `estimated_exchange_dmdt` columns including `TRP[p]` with a positive (uptake) mean: `FULLY OK: 58/58, total cells 1856, PROBLEMS 0`. 42 pathway genotypes + 16 native designs on simulator **204** (`42b1eac2` → v2ecoli `136f6af5` → ecoli-sources `86607c4d`), parquet, caches staged `47ea00f` → `42b1eac`. **Cost:** two gangs lost mid-flight to dead Ray nodes (db 1242 g22 at gen 5, db 1252 g32 at gen 2 with two nodes down) — infrastructure, with spot reclaim (on-demand CE), root disk (200 GB) and the solver each ruled out from the traceback; both re-fired (db 1278/1279) and both landed, which is why the tally is 58 not 56. **Two results carry forward:** (1) **genotypes 6 and 38 both completed** — the pair written off as permanently failing on v2ecoli#784 — so the pathway +Trp arm is **42/42** against the minimal arm's 40/42; all four prior failures were under a minimal uptake set (the two labelled `with_trp` never imported tryptophan), both completions under a real one; no mechanism claimed and the minimal-arm failures stand. (2) **Trp uptake is structured across genotypes**: g7–21 tightly at +5.0e4..+5.4e4 (±4% over fifteen), g5/6/23/24/25/26 ~4× higher at +1.9e5..+2.2e5, **native design 1 at +3.7e3, an order of magnitude below any pathway genotype** — measurement only, handed to Chris/Eran as the axis Run 4 is built around. Roster decision (do g6/g38 re-enter the comparison) is theirs; open on my side is re-running Eran's fss combine once `_splice_check` is fixed |
